# Dependencias solo de desarrollo y pruebas.
# api/requirements.txt es lo que instala Vercel: nada de esto va ahi.
#
#   pip install -r requirements-dev.txt
#
-r api/requirements.txt

pytest>=8.0
httpx>=0.27,<0.28    # TestClient de FastAPI -- 0.28 quito el atajo app= que usa
                     # el TestClient de starlette 0.36.x (el que trae fastapi==0.110.0);
                     # sin este techo, una instalacion nueva (como CI, que no tiene cache)
                     # falla con TypeError: Client.__init__() got an unexpected keyword
                     # argument 'app' en todos los TestClient del proyecto
pyflakes>=3.2        # guarda de nombres indefinidos e imports muertos

# Opcional: revision visual del backoffice (capturas y deteccion de desbordes).
# Sin esto las pruebas siguen pasando; solo se pierde esa comprobacion manual.
#   pip install playwright && python -m playwright install chromium
