# Reservas de carril

**Se reserva ANTES de escribir la primera línea.** Si el carril que quieres ya tiene una
reserva `en curso` a nombre de otro, no lo cojas: coge el siguiente libre.

Tu nombre tiene que ser **único**. Si dos agentes van con el mismo nombre, la reserva de uno
le aparece al otro como propia y este fichero deja de proteger nada. Si no te han dado
nombre, pídelo antes de escribir aquí.

Al terminar, cambia tu estado a `terminado` y añade el sha del último commit.

| Carril | Agente | Desde | Estado | Commit |
|---|---|---|---|---|
| W0 | agente-w0-hemorragia | 2026-09-03 | terminado | c54c29c |
| W1 | agente-w1-redseguridad | 2026-09-03 | terminado (parcial: IN-001) | 3851a1e |
| W1b | agente-w1b-fixture-ci | 2026-09-03 | terminado | f2a5b10 |
| O1 (H1c-autorizacion) | agente-o1-autorizacion | 2026-09-03 | terminado | 9ff1fc1 |
| O2 (H1b-conexion) | agente-o2-transacciones | 2026-09-03 | terminado | defd015 |
| C1-docs-backend | agente-c1-docs | 2026-09-03 | terminado | 66c0b2b |
| C2-catalogo | agente-c2-catalogo | 2026-09-03 | terminado | 28bbff1 |
| C3-stats-backend | agente-c3-stats | 2026-09-03 | terminado | 49e755e (ver nota en _BUZON.md: primer intento de commit, d71096c, quedo mal formado por una carrera de git concurrente con A4-rrhh-backend) |
| C4-retencion | agente-c4-retencion | 2026-09-03 | terminado | 249dba7 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente con C2-catalogo, contenido correcto) |
| C6-usuarios-backend | agente-c6-usuarios | 2026-09-03 | terminado | d4f4ae8 (test_autorizacion_usuarios.py cayó en el commit 28bbff1 de C2-catalogo por la misma carrera de git concurrente; ver nota en _BUZON.md, contenido correcto) |
| C7-papelera | agente-c7-papelera | 2026-09-03 | terminado | 42b90c5 |
| A2-archivo-backend | agente-a2-archivo-backend | 2026-09-03 | terminado | 19e86f1 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente, contenido correcto) |
| A4-rrhh-backend | agente-a4-rrhh-backend | 2026-09-03 | terminado | d71096c (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente, contenido correcto) |
| B4-admin-tabs | agente-b4-admin-tabs | 2026-09-03 | terminado | 6c55738 (ver nota en _BUZON.md: primer intento de commit, 88d0089, quedó mal formado por una carrera de git concurrente con D1-ia-backend; contenido correcto) |
| B5-admin-monitor | agente-b5-admin-monitor | 2026-09-03 | terminado | 5228225 |
| B6-admin-ui | agente-b6-admin-ui | 2026-09-03 | terminado | 0ebba97 |
| B7-admin-submit | agente-b7-admin-submit | 2026-09-03 | terminado | e98de09 |
| B8-admin-edit-archivo | agente-b8-admin-edit-archivo | 2026-09-03 | terminado | 7e2afb6 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente con "chore: reservar H3-pruebas-legacy...", contenido correcto) |
| B9-admin-edit-rrhh | agente-b9-admin-edit-rrhh | 2026-09-03 | terminado | 2fcb9d4 (ver nota en _BUZON.md: OR-005/OR-006 quedaron en un commit compartido por una carrera de git concurrente con B12-admin-usuarios, contenido correcto; OR-009 no era de este carril, redirigido a C7-papelera, ya resuelto en 42b90c5); 147e0fc documenta la carrera |
| B10-admin-charts | agente-b10-admin-charts | 2026-09-03 | terminado | 3b6001e (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente con B6-admin-ui, contenido correcto) |
| B11-admin-categorias | agente-b11-admin-categorias | 2026-09-03 | terminado | f0359bc |
| B12-admin-usuarios | agente-b12-admin-usuarios | 2026-09-03 | terminado | 2fcb9d4 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente, contenido de admin-users.js correcto) |
| D1-ia-backend | agente-d1-ia-backend | 2026-09-03 | terminado | a3052c6 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente con C10-ficheros-r2, contenido correcto) |
| D2-ia-frontend | agente-d2-ia-frontend | 2026-09-03 | terminado | 2fcb9d4 (ver nota en _BUZON.md: commit ajeno de agente-b12-admin-usuarios que arrastró mis tres archivos por una carrera de git; contenido correcto — SD-041, SI-017, SI-005) |
| C9-auth | agente-c9-auth | 2026-09-03 | terminado | de9f6cf (primer intento, 9244e45, quedó absorbido por una carrera de git con F1-paginas-estaticas — ver nota en _BUZON.md; de9f6cf sí contiene mi trabajo, verificado con git show/grep) |
| C10-ficheros-r2 | agente-c10-ficheros-r2 | 2026-09-03 | terminado | a3052c6 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente con D1-ia-backend, contenido correcto) |
| F1-paginas-estaticas | agente-f1-paginas | 2026-09-03 | terminado | 9244e45 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente con C9-auth, contenido correcto) |
| F2-cascara | agente-f2-cascara | 2026-09-03 | terminado | 251a141 |
| E2-escaner-cliente | agente-e2-escaner-cliente | 2026-09-03 | terminado | 56e7be0725f24bdf4e61442b57eb0525b0f26fa |
| H3-pruebas-legacy | agente-h3b-legacy-fixtures | 2026-09-03 | terminado | a4d7913 (test_admin.py en b1793c2) |
| C5-importaciones | agente-c5b-importaciones | 2026-09-03 | terminado | be77391 (ver nota en _BUZON.md: commit ajeno de agente-c8b-backup que arrastró imports.py y test_imports.py por una carrera de git concurrente; contenido correcto, verificado con git diff y pytest en verde) |
| C8-backup | agente-c8b-backup | 2026-09-03 | terminado | f989f19 |
| BR-109 | agente-br109-dossier-estilos | 2026-09-03 | terminado | 6284460 |
| L0-tokens | agente-l0-tokens | 2026-09-03 | terminado | 3b6e18b (ver nota en _BUZON.md: commit ajeno de agente-h2-app-js que arrastró mi styles.css por una carrera de git concurrente; contenido correcto, verificado con git show) |
| H2-app-js | agente-h2-app-js | 2026-09-03 | terminado | 5b566e4 |
| verificacion-despliegue | agente-verificacion-despliegue | 2026-09-03 | terminado | 4f1f52d |
| L1-estilos | agente-l1-estilos | 2026-09-03 | terminado | b93bdb4 (ver nota en _BUZON.md: mi trabajo quedó arrastrado por el commit de agente-l2-estilos por una carrera de git concurrente en el árbol compartido, contenido verificado con `git show b93bdb4 -- app/static/styles.css`) |
| L2-estilos | agente-l2-estilos | 2026-09-03 | terminado | b93bdb4 |
| L3-estilos | agente-l3-estilos | 2026-09-03 | terminado | 6ca0fc7 |
| L4-estilos | agente-l4-estilos | 2026-09-03 | terminado | 7de4937 (ver nota en _BUZON.md: mi trabajo quedó arrastrado por el commit de agente-l10-estilos por una carrera de git concurrente en el árbol compartido, contenido verificado con `git show 7de4937 -- app/static/styles.css`) |
| L5-estilos | agente-l5-estilos | 2026-09-03 | terminado | ada29aa (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente, contenido correcto) |
| L6-estilos | agente-l6-estilos | 2026-09-03 | en curso | |
| L6-estilos | agente-l6b-estilos-focused | 2026-09-03 | terminado (95fc341) | |
| LT | agente-lt-tests-vercel | 2026-09-03 | terminado | d85a26f |
| LX | agente-lx-reestructuracion | 2026-09-03 | terminado (parcial: SD-187, ver nota en _BUZON.md) | ee18da7 |
| LG | agente-lg-galeria | 2026-09-03 | terminado (SD-223: `/sistema` con tokens de L0 en vivo y los componentes reescritos hasta ahora; enlace de menú lateral pendiente, ver nota en `_BUZON.md` — requiere `app.js`, fuera de este carril) | `72b7e7e` |
| E1-escaner-puente | agente-e1-escaner-puente | 2026-09-03 | terminado (parcial: 24 de 107 fichas — seguridad/protocolo/decodificador HID/pruebas de `scanner-app/server.js`; el resto son subsistemas nuevos de esfuerzo L —Electron, TWAIN/WIA/ICA/SANE, OCR, lotes, preservación— sin decisión de producto tomada, documentados en `scanner-app/README.md`) | 0eaf779 |
| H1a-migraciones | agente-h1a-migraciones | 2026-09-03 | terminado | `2828d16` |
| H1d-modelos | agente-h1d-modelos | 2026-09-03 | terminado | ab84ecc |
| H1e-consultas | agente-h1e-consultas | 2026-09-03 | terminado | 9a3a2e3 |
| H1f-rutas-pagina | agente-h1f-rutas-pagina | 2026-09-03 | terminado (sin cambios de código: `pages.py`/`config.py` ya cumplían, todo lo pendiente es `[CHOCA]` y quedó anotado en `_BUZON.md`) | e5a7696 |
| H4-despliegue | agente-h4-despliegue | 2026-09-03 | terminado (parcial: 6 de 37 fichas resueltas de config/despliegue real; el resto exige main.py/database.py/backup.py/.python-version/CI/paneles externos, ver nota en _BUZON.md) | 8a98120 |
| A1-buscador-archivo | agente-a1-buscador-archivo | 2026-09-03 | terminado | 795cd42 |
| A3-buscador-rrhh (hr.js) | agente-a3a-buscador-rrhh-js | 2026-09-04 | terminado (interrumpido por corte de sesion, retomado por el orquestador: comiteado tal cual quedo, ver ab3be26) | ab3be26 |
| A3-buscador-rrhh (hr.html) | agente-a3b-buscador-rrhh-html | 2026-09-04 | terminado | db8679e |
| PASS2-admin-edit | agente-pass2-admin-edit | 2026-09-04 | terminado (OA-046, OR-037, OR-147, OR-219; resto verificado ya resuelto o requiere `[CHOCA]`, ver _BUZON.md) | |
| PASS2-docs-hr-backend | agente-pass2-docs-hr-backend | 2026-09-04 | terminado (parcial: sólo `hr.py` tenía tickets abiertos y accionables en solitario — BR-003, BR-005, BR-006, BR-015, BR-059, BR-062, BR-057; `docs.py` no tenía pendientes sin marcar accionables sólo con ese archivo, ver nota en `_BUZON.md`) | 3e9dc0d |
| PASS2-archive-depth | agente-pass2-archive-depth | 2026-09-04 | terminado (BA-005, BA-015, BA-016, BA-026, BA-027, BA-161, BA-167; ver nota en _BUZON.md) | 13bc2ce |
| PASS2-engineering | agente-pass2-engineering | 2026-09-04 | terminado | f264057 |
| PASS2-ai | agente-pass2-ai | 2026-09-04 | terminado (SI-019, SI-020, SI-061, SI-066; resto confirmado ya resuelto o documentado como pendiente en _BUZON.md) | 20548d9 (mi commit quedó absorbido por una carrera de git concurrente con PASS2-archive-depth; contenido verificado con `git show 20548d9:app/core/ai_tools.py`) |
| PASS2-hr-frontend-depth | agente-pass2-hr-frontend-depth | 2026-09-04 | terminado | 15ede7f |
| PASS2-models-schema | agente-pass2-models-schema | 2026-09-04 | terminado (sin cambios de código: RQ-009/RQ-010/RQ-014/RQ-016/RQ-028/RQ-041/RQ-044/RQ-049 revisados contra `models.py`/`schema.sql` — todo lo accionable ya estaba resuelto por H1d-modelos, el resto exige `main.py`/`hr.py`/`admin/*.py` fuera de zona; hallazgo real de RQ-049 documentado en `_BUZON.md`) | bbb2442 |
| SWEEP-styles | agente-sweep-styles | 2026-09-03 | terminado | d07452e |
| LX-2 | agente-lx2-modularizacion | 2026-09-03 | terminado (parcial: solo SD-214 hecho; SD-211/212/213/206 sin tocar, bloqueo real y numeros actualizados en _BUZON.md) | baebd7c |
| LX-3 | agente-lx3-modularizacion | 2026-09-04 | terminado (SD-213 completo: 12 modulos reales bajo app/static/styles/, mas un bug real de @layer que invalidaba los @import corregido por el orquestador tras el corte de sesion — ver commit; SD-212/SD-211/SD-206 quedan para otra pasada) | 9991649 |
| LX-4 | agente-lx4-orden-important | 2026-09-04 | terminado (parcial: SD-212 solo `componentes.css` — comentarios de sección sin mover reglas de sitio, por el riesgo de empate de especificidad; SD-206 solo `componentes-admin.css` — el bloque de acento por tema de 11 selectores repetidos por propiedad consolidado con `:is()`, misma especificidad; SD-211 solo la familia `.card`/`.card-header`/`.card-title`/`.card-body`/`.card-outline` en `componentes.css`, 14 `!important` retirados tras confirmar que Bootstrap vive en su propia `@layer` (SD-187) y la capa `app` ya gana por orden de capa sin necesitarlo. Detalle completo y alcance restante en `_BUZON.md`, sección LX-4) | 7d120d8 |
| LX-5 | agente-lx5-important-resto | 2026-09-04 | terminado (parcial: 29 `!important` retirados de 613 en los 10 modulos restantes — base.css 2, shell.css 8, impresion.css 2, componentes-admin.css 14 (resto, fuera del acento de tema que ya hizo LX-4), paneles-admin.css 3; quedan 584. tokens.css y menus.css ya estaban en 0. responsive.css, personalizacion.css, dark-mode.css y paginas.css sin cambios: sus `!important` compiten con clases de utilidad de Bootstrap (`.bg-*`, `.text-*`, `.d-flex`...), que SI llevan `!important` en Bootstrap 4.6 — ahi quitarlo invierte quien gana, no es el mismo caso que `.card`/`.btn`/`.navbar`/`.form-group` sin `!important` en origen. Tambien se dejaron intactos los bloques `body.theme-*`/`body.dark-mode`, los empates de especificidad documentados en el propio CSS (SD-093/SD-095/SD-105/SD-130), flatpickr (libreria sin `@layer`) y las sobre-escrituras de `style` en linea. Detalle completo en el informe final del agente) | 917f9d8, d29abed, 80c2da6, fc0ac99 |
| SWEEP-main | agente-sweep-main | 2026-09-03 | terminado | 5ac751c |
| SWEEP2-styles | agente-sweep2-styles | 2026-09-04 | terminado (sin cambios de código: las 3 entradas sin marcar con archivo `app/static/styles.css` — línea 403 OA-201/OR-234/OR-077/OA-210/OR-280, línea 662 OR-241, línea 1451 L14-estilos — exigen todas HTML/JS/backend fuera de mi zona (`app/static/styles/*.css`), ya documentado por agentes previos CSS-only; ver nota en _BUZON.md) | 3e0e9a6 |
| SWEEP2-admin-html | agente-sweep2-admin-html | 2026-09-04 | terminado (parcial: 5 de 9 peticiones sin marcar resueltas — SD-138, OR-009 y OA-181 confirmadas ya cerradas por otros carriles, 2 entradas duplicadas marcadas; el resto exige backend/JS/CSS fuera de zona, ver nota en `_BUZON.md`) | 80b1a8c |
| SWEEP2-admin-monitor-js | agente-sweep2-admin-monitor-js | 2026-09-04 | terminado | e7f5690 |
| SWEEP2-main-schema | agente-sweep2-main-schema | 2026-09-04 | terminado (IN-034, SI-031 parte de schema.sql) | ada3784 |
| SWEEP2-backup-docs-deps-ai | agente-sweep2-backup-docs-deps-ai | 2026-09-04 | terminado (parcial: 1 de ~8 pendientes sin marcar era realmente accionable sólo con mis cuatro archivos — OA-104/OR-128 en `docs.py`; el resto exigía tocar `main.py`/`admin/users.py`/`storage.py`/HTML ajenos o `deps.py` con cambios de autorización que preferí no arriesgar, ver `_BUZON.md`) | 03fa263 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente con SWEEP2-styles, contenido correcto) |
| SWEEP2-shell-app | agente-sweep2-shell-app | 2026-09-04 | terminado (2 peticiones sin marcar dirigidas a app.js/app-shell.js: SI-044/SI-046 resueltas y SI-043 parcial en app.js; RQ-053 en app-shell.js sigue bloqueada, `inicio.html` no existe todavía, ver _BUZON.md) | 21bf3d3 |
| SWEEP2-misc | agente-sweep2-misc | 2026-09-04 | terminado (3 de 9 tickets del buzón eran falsos positivos ya cerrados por otros carriles — DG-169, SI-235, C4-retencion/test_misc.py, marcados `[x]`; BA-112 en `archive.html` ya estaba resuelto; SI-113/115/116/117/119/122/125 aplicadas en `ai-widget.js`; login.html/ayuda.html/hr.html/scanner-client.js no tenían pendientes accionables sólo con mi archivo, ver `_BUZON.md`) | 142b240 |
| SWEEP-admin-docs | agente-sweep-admin-docs | 2026-09-03 | terminado | f5cfa7f |
| SWEEP3-rutas-backend | agente-sweep3-rutas-backend | 2026-09-04 | terminado (parcial: 1 de ~8 pendientes sin marcar en mis siete archivos era realmente accionable de bajo riesgo — IN-156 en auth.py; el resto exige tocar HTML/JS ajeno o es decision de producto/backend nuevo de esfuerzo M, ver `_BUZON.md`) | a459cb4 |
| SWEEP3-html-js-resto | agente-sweep3-html-js-resto | 2026-09-04 | terminado (parcial: 6 fichas confirmadas ya resueltas por commits previos — OA-015, OA-055, OA-053, OR-009, bloque login SI-032/034/038/049-052/054 — sin cambios de código; resto de lo sin marcar exige backend/styles.css/admin.js/app.js ajenos, ver nota en _BUZON.md) | 5da87d6 |
| SWEEP-admin-html | agente-sweep-admin-html | 2026-09-03 | terminado | 77c8d74 |
| SWEEP-monitor-backup | agente-sweep-monitor-backup | 2026-09-03 | terminado (parcial: 3 de ~10 peticiones resueltas en stats.py/backup.py; admin-monitor.js y admin/deps.py sin peticiones accionables, ver nota en _BUZON.md) | b278648 |
| SWEEP-shell-core | agente-sweep-shell-core | 2026-09-03 | terminado | (ver commit en _BUZON.md) |
| SWEEP-admin-js | agente-sweep-admin-js | 2026-09-03 | terminado | 1651fa3 |
| SWEEP-misc-paginas | agente-sweep-misc-paginas | 2026-09-03 | terminado | b59ee5c |
| SWEEP-tests-nuevos | agente-sweep-tests-nuevos | 2026-09-03 | terminado | ff4c076 (nota en `_BUZON.md` quedó en el commit `5ac751c` de agente-sweep-main por una carrera de git concurrente, contenido correcto) |
| L7-estilos | agente-l7-estilos | 2026-09-03 | terminado | 2bc5690 (ver nota en _BUZON.md: mi cambio de styles.css quedó arrastrado por el commit de agente-la-asistente por una carrera de git concurrente en el árbol compartido, contenido verificado con `git show 2bc5690 -- app/static/styles.css`) |
| L8-estilos | agente-l8-estilos | 2026-09-03 | terminado | ada29aa |
| L9-estilos | agente-l9-estilos | 2026-09-03 | terminado | 6ca0fc7 (ver nota en _BUZON.md: mi trabajo quedó absorbido por el commit "L3-estilos" de otro agente por una carrera de git concurrente en el árbol compartido; mi propio intento de commit, 7194678, sólo capturó 1 línea residual. Contenido verificado con grep de mis marcadores SD- en HEAD tras 6ca0fc7 y con `python -m pytest app/tests -q` en 713/0) |
| L10-estilos | agente-l10-estilos | 2026-09-03 | terminado | 7de4937 (ver nota en _BUZON.md: commit tomó el árbol de trabajo compartido con otros lotes L1-L15 editando styles.css a la vez, mi cambio verificado correcto dentro de él) |
| L11-estilos | agente-l11-estilos | 2026-09-03 | terminado | b93bdb4 (ver nota en _BUZON.md: mi trabajo quedó arrastrado por el commit de agente-l2-estilos por una carrera de git concurrente en el árbol compartido, contenido verificado con `git log -S"ds-accent-ink"` + `git show b93bdb4:app/static/styles.css`) |
| L12-estilos | agente-l12-estilos | 2026-09-03 | terminado | 7de4937 (ver nota en _BUZON.md: mi trabajo quedó arrastrado por el commit de agente-l10-estilos por una carrera de git concurrente en el árbol compartido, contenido verificado con `git show 7de4937:app/static/styles.css`) |
| L13-estilos | agente-l13-estilos | 2026-09-03 | terminado | 7de4937 (ver nota en _BUZON.md: mi cambio en la zona "CAPA DE MOVIMIENTO" quedó arrastrado por el commit de agente-l10-estilos por una carrera de git concurrente en el árbol compartido, contenido verificado con `git show 7de4937 -- app/static/styles.css`) |
| L14-estilos | agente-l14-estilos | 2026-09-03 | terminado | 2bc5690 (ver nota en _BUZON.md: mi trabajo quedó arrastrado por el commit de agente-la-asistente por una carrera de git concurrente en el árbol compartido, contenido verificado con `git show 2bc5690:app/static/styles.css` — sigue vivo en HEAD `ada29aa`) |
| L15-estilos | agente-l15-estilos | 2026-09-03 | terminado | 7de4937 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente con L10-estilos, contenido correcto) |
| LH-paginas | agente-lh-paginas | 2026-09-03 | terminado (parcial: SD-026, SD-027, SD-028, SD-209) | a02a2ef |
| LA-asistente | agente-la-asistente | 2026-09-03 | terminado | 65fdf78 (ver nota en _BUZON.md: commit ajeno de agente-verificacion-despliegue que arrastró mi ai-widget.css por una carrera de git concurrente, contenido correcto) |
| LW-www | — | 2026-09-03 | terminado | ya resuelto en 9244e45 (F1-paginas-estaticas borró www/styles.css) |

| PASS3-rrhh-backend-untouched | agente-pass3-rrhh-backend | 2026-09-04 | terminado (interrumpido por limite de sesion, rescatado y comiteado por el orquestador tras verificar sintaxis y pytest: OR-021/022/023, BR-063/064/065/067/068/069, OR-013/014/037/038/044, OA-006/007, OA-042) | 54237d8 |
| PASS3-catalog-retention-imports | agente-pass3-catalog-retention-imports | 2026-09-04 | terminado (interrumpido por limite de sesion, rescatado por el orquestador; imports.py/helpers.py sin cambios en lo rescatado) | 79472a6 |
| PASS3-admin-js-untouched | agente-pass3-admin-js-untouched | 2026-09-04 | terminado (interrumpido por limite de sesion, rescatado por el orquestador: admin-ui.js y admin-users.js; admin.js/admin-submit.js/admin-stats.js/admin-categories.js sin cambios en lo rescatado — relanzar si hace falta profundizar) | 05e812b |
| PASS3-styles-buzon-2 | agente-pass3b-styles-buzon | 2026-09-05 | terminado (sin cambios de código: reverificadas las mismas 11 entradas sin marcar de `app/static/styles.css`, 0 pasaron a accionable — ver nota en `_BUZON.md`) | |
| PASS3-admin-html-depth-2 | agente-pass3b-admin-html-depth | 2026-09-05 | terminado (0 de las entradas sin marcar de `admin_hr.html`/`admin_archive.html` pasaron a accionable; `docs.py`/`trash.py`/`retention.py`/`admin.js`/`admin-edit-hr.js` no añadieron los endpoints de lote ni la rama de `loadAdminTab` que esos bloqueos necesitaban — ver nota en `_BUZON.md`) | 78f5618 (ver nota en _BUZON.md: commit compartido por una carrera de git concurrente, admin-submit.js de otro agente arrastrado; mi contenido verificado con `git show 78f5618 -- docs/auditoria/_BUZON.md`) |
| PASS3-admin-js-resto | agente-pass3-admin-js-resto | 2026-09-05 | terminado (OR-035 en `admin-submit.js`: el catch de `handleNewSubmission` ahora marca el campo culpable, no sólo el toast; el resto de tickets revisados en `admin.js`/`admin-stats.js`/`admin-categories.js`/`admin-submit.js` ya estaban resueltos por PASS3-admin-js-untouched u otros carriles previos, verificado contra el código actual; lo que sigue abierto exige HTML/backend fuera de zona, ver `_BUZON.md`) | 78f5618 (mi commit quedó absorbido por una carrera de git concurrente con la reverificación de `_BUZON.md`; contenido verificado con `git show 78f5618 -- app/static/admin-submit.js`) |
| PASS3-login-scanner-js | agente-pass3-login-scanner-js | 2026-09-04 | terminado (SI-175 corregido — id de selector muerto; SI-177/DG-162 parcial — accesibilidad de teclado; login.js sin cambios, ya resuelto por C9-auth; resto documentado en _BUZON.md) | b68a951 |
| RONDA5-ai-proposals-crear | agente-ronda5 | 2026-09-07 | terminado (accion "crear" de ai_proposals.py sin ningun test: 5 tests nuevos en test_ia.py — INSERT en Archivo, RRHH con/sin empleado_id, normalizacion de vacio a NULL, recorrido completo desde approve()) | fd64c9e |

## Cómo se escribe una fila

**Nota de orquestación (2026-09-04)**: este bloque de ejemplo tenía la fila
`A3-buscador-rrhh | bruno | ...` como texto de muestra, con un sha de relleno
(`a1b2c3d`). Varios agentes la confundieron con una reserva real y respetaron
el carril "ocupado" durante más de 30 horas sin que nadie lo estuviera
trabajando de verdad. Limpiado: el ejemplo ya no usa un nombre de carril real
para no repetir la confusión.

```
| <carril> | <tu-nombre-de-agente> | <fecha> | en curso | |
| <carril> | <tu-nombre-de-agente> | <fecha> | terminado | <sha-del-commit> |
```

## Carriles que no se cogen sin permiso

- `W0`, `W1`, `H1c`, `H1b` — van primero y solos; el resto depende de ellos.
- `LX` — va el último de todos: parte `styles.css` en módulos e invalida los rangos de línea
  con los que trabajan los demás lotes de diseño.
- `L0` y `BR-109` — van antes que cualquier otro trabajo estético.
| SUPERVISOR-coherencia | agente-supervisor-coherencia | 2026-09-05 | terminado | 7b3a391 |
| VI-app-theme-escape | agente-vi-app-theme | 2026-09-05 | terminado | 15f40cf |
| VI-styles-desbordes | agente-vi-styles-desbordes | 2026-09-05 | terminado (VI-007 corregido — causa estructural de VI-005/VI-006 —, VI-018 y VI-009; VI-013/VI-014/VI-020 confirmados ya resueltos por trabajo previo; VI-004/VI-008 no eran de mi zona o ya estaban resueltos, ver nota en _BUZON.md) | 4c6436a |
| VI-archive-hr-js | agente-vi-archive-hr-js | 2026-09-05 | terminado (VI-005/VI-006 recortados por JS puro en archive.js/hr.js; VI-015 ya estaba resuelto en código actual; VI-012 no accionable solo con mis dos archivos, ver nota en _BUZON.md) | a61f70a |
| VI-admin-ai-html | agente-vi-admin-ai-html | 2026-09-05 | terminado (VI-010: sólo faltaba envolver `gasto-tabla` y `conv-tabla`, las otras dos ya tenían `.table-responsive`; VI-011 ya estaba resuelto, sin cambios de código, ver nota en `_BUZON.md`) | 3780b63 (mi commit quedó absorbido por una carrera de git concurrente con "VI-app-theme-escape terminado"; contenido verificado con `git show 3780b63:app/static/admin_ai.html` y `git show 3780b63:docs/auditoria/_BUZON.md`) |
| VI-ai-widget-zindex | agente-vi-ai-widget-zindex | 2026-09-05 | terminado | 3780b63 (mi commit quedó absorbido por una carrera de git concurrente con "VI-app-theme-escape terminado"; contenido verificado con `git show 3780b63 -- app/static/ai-widget.css app/static/ai-widget.js`) |
| VI-charts-nan | agente-vi-charts-nan | 2026-09-05 | terminado (interrumpido por corte de red, rescatado y comiteado por el orquestador tras verificar sintaxis y pytest) | 3ca6bbb |
| VI-admin-js-datos | agente-vi-admin-js-datos | 2026-09-05 | terminado (interrumpido por corte de red, rescatado por el orquestador: VI-028/030/033/034/035) | 0cac70b |
| VI-archive-hr-datos | agente-vi-archive-hr-datos | 2026-09-05 | terminado (VI-031, VI-032, VI-037 — ver nota en _BUZON.md) | fb4463e |
| VI-admin-ai-datos | agente-vi-admin-ai-datos | 2026-09-05 | terminado (interrumpido por corte de red, rescatado por el orquestador; comiteado junto con VI-charts-nan por carrera de git) | 3ca6bbb |
| VI-styles-oscuro-restante | agente-vi-styles-oscuro | 2026-09-05 | terminado (interrumpido por corte de red, rescatado por el orquestador) | e28fa8e |
| VI-focus-touch | agente-vi-focus-touch | 2026-09-05 | terminado (interrumpido por corte de red, rescatado por el orquestador: VI-044/VI-045) | 9de46f8 |
| VI-nombres-consistentes | agente-vi-nombres | 2026-09-05 | terminado (interrumpido por corte de red, rescatado por el orquestador: VI-050) | eb33cda |
| VI-archive-hr-cleanup2 | agente-vi-archive-hr-cleanup2 | 2026-09-05 | terminado (interrumpido por corte de internet, rescatado y comiteado por el orquestador tras verificar sintaxis, pytest y test_visual.py) | 2f48119 |
| VI-admin-forms | agente-vi-admin-forms | 2026-09-05 | terminado (interrumpido por corte de internet, rescatado y comiteado por el orquestador tras verificar sintaxis, pytest y test_visual.py) | 521c740 |
| VI-admin-js-misc2 | agente-vi-admin-js-misc2 | 2026-09-05 | terminado (interrumpido por corte de internet, rescatado y comiteado por el orquestador tras verificar sintaxis, pytest y test_visual.py) | 52491ef |
| VI-login | agente-vi-login | 2026-09-05 | terminado (interrumpido por corte de internet, rescatado y comiteado por el orquestador tras verificar sintaxis, pytest y test_visual.py) | 1c76f19 |
| VI-compartido-ayuda | agente-vi-compartido-ayuda | 2026-09-05 | terminado (interrumpido por corte de internet, rescatado y comiteado por el orquestador tras verificar sintaxis, pytest y test_visual.py) | 273000b |
| VI-shell-integration | agente-vi-shell-integration | 2026-09-05 | terminado (interrumpido por corte de internet, rescatado y comiteado por el orquestador tras verificar sintaxis, pytest y test_visual.py) | 10ea006 |
| VI-styles-final | agente-vi-styles-final | 2026-09-05 | terminado (interrumpido por corte de internet, rescatado y comiteado por el orquestador tras verificar sintaxis, pytest y test_visual.py) | e76437a |
| BA-app-choices | agente-ba-app-choices | 2026-09-05 | terminado (BA-010, BA-009, BA-027, BA-094, BA-177; BA-003/004/145/172 y la mitad de BA-009 en app.js documentados en _BUZON.md, fuera de zona) | 0cfffa5 |
| VI-viz-tokens | agente-vi-viz-tokens | 2026-09-05 | terminado (sin cambios de código: `vizSeries()` ya devuelve exactamente 8 colores en orden fijo, sin ciclar y sin inventar un noveno tono — cumple la regla de CLAUDE.md. El agrupamiento en "Otros" para VI-038/VI-039 ya lo resolvió hoy `agente-vi-charts-nan` del lado del cliente en `admin-charts.js` (`_capYColorear`, commit `3ca6bbb`), consumiendo el array de `vizSeries()` tal cual; no duplicado aquí. `node --check` OK, pytest 859/859 y test_visual.py 54/54, sin cambios respecto al inicio) | (sin commit) |
| IN-share-revocation | agente-in-share-revocation | 2026-09-05 | terminado (IN-145: claves derivadas por propósito con HMAC(secret_key, "session"/"share"); IN-146: tabla `enlaces_revocados` + `jti` aleatorio dentro del token firmado, `verify_share_token` la consulta, endpoints `/api/admin/compartir/revocar` y `/api/admin/compartir/revocados` con `require_admin_role`) | 9dc3300 |
| IN-rotacion-docs | agente-in-rotacion-docs | 2026-09-05 | terminado | 6dd4fba |
| OR-monitor-rrhh | agente-or-monitor-rrhh | 2026-09-05 | terminado (OR-127, OR-121, OR-122, OR-126, OR-125, OR-128, OR-129, OR-130 — ver nota en _BUZON.md para simplificaciones) | 83bacec |
| BR-hr-js-tercer-pase | agente-br-hr-js-3 | 2026-09-05 | terminado (bug real sin ficha propia: `_wireDossierOpenButtons` se llamaba sin estar definida en ningún archivo — `ReferenceError` en cada apertura/refiltrado del dossier, que dejaba los accesos directos de Cédula/RIF/CV/Planilla sin conectar; BR-037 residual en la foto del dossier, BR-054 debounce del filtro interno, BR-055 parcial en `_renderDossierFileList`; BR-018 a BR-025/027/034/035/038-041/043 verificados ya resueltos por A3a/PASS2-hr-frontend-depth sin tocar de nuevo; resto exige `hr.py`/`hr.html`/`app.js`/`app-core.js`/`main.py`/`models.py` fuera de zona, ver `_BUZON.md`) | d1676ee |
| BA-archive-js-tercer-pase | agente-ba-archive-js-3 | 2026-09-05 | terminado (BA-067, BA-072; resto de BA-065 a BA-100 verificado ya resuelto o bloqueado por styles.css/app-choices.js fuera de zona, ver _BUZON.md) | c4669f3 |
| OA-admin-edit-tercer-pase | agente-oa-admin-edit-3 | 2026-09-05 | terminado (OA-009, OA-043, OR-146, OR-153, y bug real sin ficha propia: `exportAdminCSV` duplicado en `admin-edit-hr.js` que revertía OR-137/138/139; resto verificado ya resuelto o bloqueado por `[CHOCA]`, ver `_BUZON.md`) | 5829b3d |
| OA-admin-submit-tercer-pase | agente-oa-admin-submit-3 | 2026-09-05 | terminado (sin cambios de código: las ~40/48 menciones a `admin-submit.js` en `backoffice-archivo.md`/`backoffice-rrhh.md` resultaron ser en su mayoría el mismo bloque OA-084…OA-101/OR-081…OR-099 ya resuelto por B7-admin-submit y PASS3-admin-js-resto — validación por campo con foco y `aria-describedby`, lista única de obligatorios, cédula/RIF venezolanos, coherencia de fechas, borrador en `localStorage` + `beforeunload`, subida con `XMLHttpRequest`/progreso/cancelar, permanencia en "Ingresar" tras guardar, distinción de errores por código con campo marcado (OR-035), aviso de duplicado por título, "Ubicación física" ya renombrado (OR-083), auto-búsqueda por cédula al perder el foco (OR-086) — verificado línea por línea contra el código actual, no contra la ficha. El resto de lo sin marcar (`[CHOCA]` en la propia ficha) exige `docs.py`/`models.py`/`admin_archive.html`/`admin_hr.html`/`admin-categories.js`/`app-choices.js`/`admin-ui.js`/`styles.css` fuera de zona — selector de estado en el alta (OA-020/OR-099), autocompletado de palabras clave y personas (OA-099/OA-100), alta múltiple (OR-094), separar "Fecha de Ingreso" de "Fecha del documento" (OR-082), etc. Ningún bug sin ficha propia: sin funciones referenciadas sin definir ni definiciones duplicadas en `admin-submit.js` frente al resto de `app/static/` (verificado con grep). `node --check` OK, `python -m pytest app/tests -q`: 867 passed antes y después, sin cambios) | (sin commit) |
| OA-admin-categories-tercer-pase | agente-oa-admin-categories-3 | 2026-09-05 | terminado (sin cambios de código: todo lo accionable ya lo resolvió B11-admin-categorias — escHtml en las tres ramas (OA-018/OR-056), sección de palabras clave sufijada por módulo (OA-030), uso + plazo de retención en cada fila (OA-126/OR-164/OR-165), búsqueda y orden (OA-127/OR-170), las cuatro Partes siempre visibles más "Sin clasificar" (OR-168), validación de longitud/duplicado con propagación del error real (OR-169), actualización puntual del nodo en vez de recargar la lista entera (OA-129). Verificado línea por línea contra el código actual, no contra la ficha. Sin bug sin ficha propia: sin funciones referenciadas sin definir (`apiFetchJSON`, `adminSuffixFromTab`, `isArchivoModule`, `formatAnios`, `promptModal`, `confirmModal` — todas existen una sola vez fuera de este archivo) ni definiciones duplicadas de las funciones propias en el resto de `app/static/` (verificado con grep). Lo que sigue abierto exige `catalog.py`/`main.py`/`models.py`/`admin_archive.html`/`admin_hr.html`/`admin.js`/`styles.css` fuera de zona — CRUD completo de tipos (OA-123/OR-163), columna `descripcion` (OA-124/OR-041), 409 en vez de "éxito" falso (OA-125/OR-040), unicidad por (nombre, categoria) (OR-040), colores de Parte como tokens en `styles.css` en vez de hex a mano (OR-166/OR-167), fusión de palabras clave (OA-130/OA-131), `loadAdminTab` pisando Retención (OR-042), proporción 5/7 del formulario (OA-128), naming en el HTML (OA-054/OR-171). `node --check` OK, `python -m pytest app/tests -q`: 867 passed antes y después, sin cambios) | (sin commit) |
| OA-admin-ui-tercer-pase | agente-oa-admin-ui-3 | 2026-09-05 | terminado (ver nota en _BUZON.md: la mayoria de las ~123 menciones a `admin-ui.js` en `backoffice-archivo.md`/`backoffice-rrhh.md` ya estaban resueltas por B6-admin-ui/PASS3 -trampa de foco, pila de modales, Escape/Ctrl+S con guardas, roving tabindex, aria-selected, promptModal con type password, extendSession renovando la cookie real, aviso de sesion tambien en visibilitychange-; arreglado de verdad: aria-hidden en los iconos decorativos que inyecta admin-ui.js en los paneles de Acceso/Auditoria/banner de sesion/menu rapido de estado (OA-070 y equivalente), y tooltip de [data-tip] ahora accesible por foco con role=tooltip/aria-describedby y recolocacion dentro del viewport (OA-192); bug sin ficha propia: los botones de paginacion de Auditoria sin aria-label. Pendientes por depender de otros archivos o de backend: OA-039 (medidor de fortaleza, necesita users.py+admin-users.js), OA-112/113 (menu de cambio de estado como role=menu real y motivo de rechazo, necesita styles.css+docs.py), OA-151/153 (filtros y exportacion de auditoria, necesita catalog.py), OA-157 (tarjetas apiladas en movil, necesita styles.css), OA-161 (selector de modulo de un usuario Global, requiere saber el rol en tiempo de inyeccion), OA-200 (degradado a la izquierda en la barra de pestanas, necesita clase nueva en styles.css). Sin funciones referenciadas sin definir ni definiciones duplicadas en `admin-ui.js` frente al resto de `app/static/` -verificado con grep-. `node --check` OK, `python -m pytest app/tests -q`: 867 passed antes y despues) | dc52139 |
| SI-ai-widget-tercer-pase | agente-si-ai-widget-3 | 2026-09-05 | terminado (SI-065/110/111/118/120 arreglados de verdad; SI-017/112/113/114/115/116/117/119/122/125 confirmados ya resueltos; SI-064/082/096/108/121/124 bloqueados por depender de otros archivos, ver `_BUZON.md`) | c84e7ea |
| SI-scanner-client-tercer-pase | agente-si-scanner-3 | 2026-09-05 | terminado (sin cambios de código: verificado ticket por ticket contra el código actual — DG-001 a DG-006/012/014/015/018/020 a 023/027/053/060/085 a 087/099/100/157/160 a 162/167 y SI-171 a 180/239. Lo accionable en solitario ya lo resolvieron `agente-e2-escaner-cliente` (commit `56e7be0`: backoff con techo DG-021, watchdog de silencio DG-006, distinción de causa de cierre DG-023, detección de contenido mixto antes de conectar DG-005/SI-171, asignación por propiedad SI-178, pausa de cámara por `visibilitychange`+inactividad SI-179) y `agente-pass3-login-scanner-js` (commit `b68a951`: SI-175 bug real del id muerto `#admin_search_input-*`, accesibilidad parcial de la superposición de cámara y del panel de configuración SI-177/DG-162). Confirmado con grep en todo `app/static/`: sin funciones referenciadas sin definir (`toggleScannerMode`, `openScannerConfig`, `handleScannerCode`, `_scnSave`, `_scnTest`, `_scnTestScan` sólo existen aquí) y sin definiciones duplicadas. Todo lo demás sigue bloqueado exactamente como documentó el pase anterior — protocolo `hello`/negociación de versión y emparejamiento por código (DG-001/002/053/060), cola con acuse y un solo cliente con foco (DG-008/009), puerto libre y descubrimiento (DG-017/018), normalización de cédula/RIF y renombrar "Escáner" (DG-012/015), modo "Capturar documento" (DG-085 a 100/167, esfuerzo L, archivos nuevos), catálogo de estados de fallo y panel fuera del sistema de diseño (DG-157/161, **[CHOCA]** `styles.css`), `data-origen="scan"` (DG-014, **[CHOCA]** `docs.py`), desactivar al cerrar sesión (DG-020, **[CHOCA]** `app.js`) y la prueba estática de ids (SI-239, pediría `app/tests/test_estatico_ids.py` nuevo) — todo exige `scanner-app/server.js`, backend, HTML de otros módulos o archivos nuevos fuera de esta zona. Ningún bug sin ficha propia encontrado. `node --check app/static/scanner-client.js`: sin errores. `python -m pytest app/tests -q`: 867 passed antes de empezar; sin cambios de código, no hizo falta repetirlo) | e58060d |
| OA-admin-users-tercer-pase | agente-oa-users-3 | 2026-09-05 | terminado (OA-039 desbloqueado: medidor de fortaleza de contraseña añadido en `admin-users.js` puro -sin tocar `admin-ui.js`- para el alta de usuario y el modal de cambio de contraseña; el resto de OA-039 -mínimo 12 caracteres, lista de comunes, `debe_cambiar_password`, caducidad- sigue en `core/security.py`/`routes/admin/users.py`, fuera de esta zona. VI-062 confirmado ya resuelto: no existe columna "Contraseña" en la tabla de usuarios. Sin funciones referenciadas sin definir ni definiciones duplicadas en `admin-users.js` frente al resto de `app/static/` -verificado con grep-. El resto de las ~135 menciones a `admin-users.js` en `backoffice-archivo.md`/`backoffice-rrhh.md` exige tocar `users.py`/`catalog.py`/`database.py`/`admin-ui.js`/`styles.css` en conjunto -0 tickets listan `admin-users.js` como único archivo, verificado con grep-, y ya fueron revisados exhaustivamente por pases anteriores -B12-admin-usuarios, C6-usuarios-backend, OA-admin-ui-tercer-pase-. `node --check` OK. `python -m pytest app/tests -q`: 863-864 passed en dos corridas, con 3-4 fallos no deterministas de `test_visual.py` -Playwright- en páginas distintas cada vez -login, archivo, admin/archivo, rrhh-, ninguno tocando `admin-users.js`; confirmado pre-existente comparando ambas corridas antes/después del cambio) | d24b31e |
| OA-admin-monitor-tercer-pase | agente-oa-monitor-3 | 2026-09-05 | terminado | 1ef890c |
| DG-admin-stats-tercer-pase | agente-dg-stats-3 | 2026-09-05 | terminado (sin cambios de código): tercera verificación ticket por ticket de `app/static/admin-stats.js` contra el código actual. Grep de `admin-stats.js` en los seis docs de `docs/auditoria/*.md` + `_BUZON.md`: OA-033/OA-072/OR-018/OR-025bis/OR-279/DG-139 están marcados `[CHOCA]` (piden `stats.py`/`admin-charts.js`/`admin.js`/`hr_alerts.py` a la vez) y SD-217 quedó revisado sin resolver por SWEEP-admin-js y SWEEP3-html-js-resto (no hay `style` inline `.info-box` que reemplazar hasta que existan los KPI pulsables de OR-062, fuera de este archivo). VI-028 (NaN) y VI-035/OR-234 (estados de carga/error de KPI) ya estaban resueltos en el código actual: `formatDias()` vive en `admin.js` (no aquí), y `_kpiCargando`/`_kpiError`/`_marcarKpiGridVivo` ya distinguen cargando/cero/error con `aria-live`. Búsqueda activa de funciones huérfanas: las cuatro funciones del archivo (`_marcarKpiGridVivo`, `_kpiCargando`, `_kpiError`, `loadDynamicStats`) se usan desde `admin.js`/`app.js`/su propio `onclick`, sin duplicados en el resto de `app/static/` (`_marcarCargando` vive sólo en `admin-charts.js`, llamado con guarda `typeof`). No queda nada accionable en solitario en este archivo; todo lo pendiente exige backend o `admin-charts.js`/`admin.js` de otro carril. `node --check app/static/admin-stats.js` OK. `python -m pytest app/tests -q`: 867 passed antes y después (sin cambios). | 923cf16 (sólo esta fila de `_RESERVAS.md`; ningún archivo de código modificado) |
| OA-admin-charts-tercer-pase | agente-oa-charts-3 | 2026-09-05 | terminado (OA-072 arreglado de verdad: las cifras que vienen de `/charts` -digitalizados/pendientes/vencidos/palabras clave/autores en Archivo, empleados/activos/jubilados/movimientos/próximas/sin-documentos en RRHH, y "último ingreso" en ambos- se quedaban en el guión largo del HTML si `/charts` fallaba, sin distinguirse de "vacío"; ahora usan el mismo patrón que ya tenía `admin-stats.js` para sus dos KPIs -marca `⚠`, `title` de reintento, clase `.ds-kpi-error` y `onclick` que repite `loadChartsData()`-, y `setEl()` limpia ese estado en la siguiente carga exitosa. OA-071 confirmado ya resuelto -sin cambio mío-: `_marcarKpiGridVivo()` en `admin-stats.js` ya pone `aria-live=polite` en el mismo `.ds-kpi-grid` compartido que rellena este archivo. OA-014/OR-026 -etiqueta del CSV-, OA-067 -tiempo relativo-, OA-074/075 -barra de digitalización accesible y color por clave-, OA-076/OR-078 -aria-label resumen en los tres canvas de Archivo y los seis de RRHH-, OA-077 -un solo mes con ingresos-, OR-073/074 -cobertura y donuts con una sola categoría-, OR-110 -alerta de importación no miente-, y VI-036/038 a VI-043 -daltonismo, resize, tooltips, leyenda- verificados ya resueltos por B10-admin-charts/VI-charts-nan, sin regresión. Sin funciones referenciadas sin definir (`handleImportCSV` es la única invocada desde `onclick` en `admin_archive.html`/`admin_hr.html`, existe una sola vez) ni definiciones duplicadas en el resto de `app/static/` -verificado con grep-. Resto de menciones -OA-068/069/070/073, OR-071/072/075 a 080, OR-101/102/103/106 a 109/111/112/113, OA-102/201- exigen `admin_archive.html`/`admin_hr.html`/`stats.py`/`imports.py`/`docs.py`/`styles.css` a la vez, marcados `[CHOCA]` en su propia ficha, documentados ya por B10-admin-charts en `_BUZON.md`, reconfirmados hoy sin novedad. `node --check app/static/admin-charts.js` OK. `python -m pytest app/tests -q`: 867 passed antes y después) | c58a993 |
| SD-app-js-tercer-pase | agente-sd-appjs-3 | 2026-09-05 | terminado (arreglado de verdad: VI-045/SD-203 -inert en el menú lateral cerrado y en el contenido tapado por el velo cuando está abierto, guardado por `matchMedia(min-width:1200px)` porque desde ahí el menú es fijo sin velo-, BA-146 -`_accesoPaginaPermitido()` comprueba el permiso antes de pintar la credencial y disparar `loadDynamicChoices()`-, SI-034 -`performLogin()`/`showLoginError()` y sus listeners eran una segunda implementación del login que `login.html` nunca ejecuta, retirada entera-, BA-176 -`parseInt` con base 10 en los cinco usos-, OA-032 -debounce 350ms en la búsqueda de auditoría, mismo patrón que ya tenía OA-031/monitor-. Verificado ya resuelto sin tocar nada: SI-043/044/046/185 -comentarios propios confirman el arreglo de sweep2-shell-app/sweep-shell-core-, BA-047 -la migaja "Comunidades" ya no existe en ningún HTML, todo el bloque de migajas se retiró en la reescritura de la cáscara; `_BREADCRUMBS`/`nav-section-breadcrumb` quedan como remanente inofensivo sin consumidor en el DOM, sin ficket propio para justificar tocarlo-. `node --check app/static/app.js` OK. `python -m pytest app/tests -q`: 867 passed. `python -m pytest app/tests/test_visual.py -q`: 1 fallo suelto por timeout de red en `admin/ia-768px-claro` bajo carga concurrente, 54/54 en verde al repetir en solitario) | 0e44d57 |
| SD-app-core-tercer-pase | agente-sd-appcore-3 | 2026-09-06 | terminado | 9fb509e |
| SD-app-shell-tercer-pase | agente-sd-shell-3 | 2026-09-06 | terminado (SI-184 -secciones del menú de `<div>`+`<a>` sueltos a `<h2>`+`<ul>`/`<li>`, sin tocar styles.css- y VI-002 -`#ds-toast-container` no existía fuera de los 4 paneles admin, `showToast()` se callaba en `/archivo`,`/rrhh`,`/sistema`; ahora `app-shell.js` lo inyecta con guarda anti-duplicado-. Verificado ya resuelto sin tocar nada: SI-043 parcial, SI-097/098, SI-181/183/185, SI-200, SD-040/131, SI-060, VI-044/045/053. Pendiente fuera de zona: RQ-053 -bloqueada, `inicio.html` sigue sin existir-, SI-037 -cambiar contraseña, backend-, SI-159/SD-207 -ayuda/investigacion sin cáscara, HTML ajeno-, SI-176 -escáner acoplado a `.ds-nav-user`, decisión de extensión no forzada-. `node --check`: OK. `python -m pytest app/tests -q`: 867 passed (antes y después). `python -m pytest app/tests/test_visual.py -q`: 54 passed) | 7f6a410 |
| REORG-static-fase1-css | agente-luisdavid-reorg | 2026-09-06 | terminado (ai-widget.css movido a styles/, 6 HTML actualizados, globs no recursivos de test_tokens.py y test_selectores_tema.py corregidos a **/*.css, 867+54 en verde, desplegado y verificado) | d326859 |
| REORG-static-fase2-js | agente-luisdavid-reorg | 2026-09-06 | terminado (20 .js movidos a js/{core,admin,archive,hr,login,ai,scanner}/, 10 renombrados dentro de admin/ quitando el prefijo redundante; 11 HTML actualizados; 7 referencias de test corregidas: test_admin_panels.py (5), test_static_assets.py, test_tokens.py, test_contraste.py -- ninguna estaba en el inventario original del plan, aparecieron al revisar archivo por archivo. INCIDENTE: bd25676 (mover los .js) se pusheó por una reserva de otro carril antes que las referencias de HTML, produccion sirvió 404 en JS viejo unos minutos hasta edcea6d; diagnosticado en vivo por agente-bug-buscadores. Suite 849/849 + 54/54, desplegado y verificado: rutas nuevas 200, rutas viejas 404) | edcea6d |
| BUG-sidebar-scroll-admin | agente-bug-sidebar | 2026-09-06 | terminado (causa raíz doble en shell.css: el sidebar de escritorio ≥1200px usaba `position:sticky`, que sigue en el flujo normal -no `fixed`-, y al ser un `<aside height:100vh>` antes de `.content-wrapper` en el HTML -sin que `<body>` sea flex/grid- empujaba todo el contenido una pantalla entera hacia abajo, de ahí el scroll para ver cualquier cosa en /admin/sistema; pasa a `position:fixed`, que sí sale del flujo -que es lo que `.content-wrapper` ya asumía con su `margin-left:280px`-. Al pasar a `fixed` con `top:0` el sidebar -mayor z-index, para el cajón móvil- quedaba encima del navbar tapando el logo institucional -el bug reportado-; se le da `top:64px`+`height:calc(100vh - 64px)` para que empiece debajo de la barra superior, con `min-height:64px` nuevo en `.main-header.navbar` para que ese offset sea estable. De paso, a ≤575.98px el logo -38vw- y la credencial de usuario se solapaban unos px por límites vw ajustados por separado; logo bajado a 30vw. Reproducido y verificado con Playwright directo -sesión falsa en localStorage + fetch interceptado, la suite normal no llega aquí porque exige sesión real-, en 390/768/1440px, claro y oscuro, en /admin/sistema y /archivo -misma cáscara-. Ningún .js ni `<script src>` tocado. `python -m pytest app/tests -q`: 846 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed) | 26c3265 |
| BUG-buscadores-no-funcionan | agente-bug-buscadores | 2026-09-06 | terminado (era el efecto transitorio de REORG-static-fase2-js, resuelto al desplegarla, sin cambios de código propios) | |
| BUG-login-no-lee-sesion | agente-bug-login-sesion | 2026-09-06 | terminado (causa raíz real, no efecto de la reorg: `login.html` nunca cargó `app.js` -donde vive `checkPersistedSession()`-, y `login.js` no comprobaba `archive_session` al iniciar; se añade `checkExistingSession()` en `login.js`, mismo TTL de 12h y mismo endpoint `/api/auth/restore` que ya usa `app.js`, redirige con `chooseLandingPage()` ya existente en el propio archivo. Ningún otro .js ni `<script src>` tocado. `python -m pytest app/tests -q`: 846 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed. `node --check` OK) | 73291aa|
| OA-admin-index-tercer-pase | agente-oa-admin-index-3 | 2026-09-06 | terminado (arreglado de verdad: OR-183/OR-184 — `loadVencimientosTable()` mostraba documentos de Archivo bajo el título "Expedientes con Retención Vencida" de RRHH porque el único endpoint que existe es Archivo-only; ahora RRHH muestra un aviso explícito en vez de datos del módulo equivocado, bloqueado por completo en `retention.py` fuera de zona. Resto de tickets con `admin.js` en `backoffice-archivo.md`/`backoffice-rrhh.md`/`sistema-diseno.md`/`recorrido-visual.md`/`requisitos-vs-implementado.md` — OA-013, OA-017, OA-051/052/053, OA-058-063, OA-080, OA-141-150, OA-167-171, OA-207/209/210, OR-020/021/022, OR-035/036/037/042, OR-067/068/069/070, OR-089, OR-141-148/158/159, OR-183/185/186, OR-221-226, OR-234/240 — verificados línea por línea contra el código actual: ya resueltos por pases previos (B4-admin-tabs, VI-admin-js-datos, VI-admin-js-misc2, PASS3-admin-js-untouched/resto) o exigen `retention.py`/`stats.py`/`hr_alerts.py`/`admin_archive.html`/`admin_hr.html`/`admin-ui.js`/`admin-submit.js`/`styles.css`/`app.js` fuera de esta zona, ya marcados `[CHOCA]` en su propia ficha. Sin funciones referenciadas sin definir ni duplicadas frente al resto de `app/static/` (verificado con grep). `node --check` OK. `python -m pytest app/tests -q`: 846 passed antes y después. `python -m pytest app/tests/test_visual.py -q`: 54 passed) | 49f7ecd |
| BUG-menu-bugueando | agente-bug-menu | 2026-09-06 | terminado (causa real, no las sospechas iniciales: `.ds-sidebar` y `.ds-sidebar-overlay` compartian `z-index: var(--z-overlay)` desde siempre -no es regresion de los tres cambios recientes-; con el mismo nivel el orden del DOM decide el apilamiento, y `app-shell.js` pinta `#sidebar-overlay` DESPUES de `#app-sidebar` -- el velo quedaba encima de TODO el cajon abierto, no solo del contenido a la derecha, y le tapaba el clic a cualquier enlace del menu por debajo de 1200px -donde el velo existe; a partir de ahi se oculta con `display:none`-. Reproducido con Playwright -sesion falsa en localStorage + fetch de `/api/**` interceptado, contra servidor local real- en 390px y 1440px<1200 (viewport chico igual, para separar del breakpoint de escritorio): al abrir el cajon con el boton hamburguesa y hacer clic en un enlace visible, Playwright fallaba con "`#sidebar-overlay` intercepts pointer events" -- el menu se veia bien, se abria, pero ningun enlace respondia al clic, sin ningun error de consola que lo delatara, calzando con el reporte sin mas detalle. El cambio sticky->fixed y el div->ul/li de los pases anteriores se descartaron como causa tras revisar CSS y DOM: ambos son consistentes entre si, sin selectores huerfanos. `.ds-sidebar` pasa de `z-index: var(--z-overlay)` a `var(--z-modal)`, un nivel por encima del velo. `python -m pytest app/tests -q`: 845 passed, 1 fallo -sistema-1440px-claro- confirmado flaky preexistente -pasa solo en 42s-. `python -m pytest app/tests/test_visual.py -q`: 54 passed) | 7c0eeab |
| SI-admin-ai-html-tercer-pase | agente-si-adminai-3 | 2026-09-06 | terminado (36 tickets SI- verificados contra el codigo actual; solo SI-015 seguia real y se corrigio con SRI en los 3 CDN del archivo; resto ya resuelto por pases previos o bloqueado por backend/styles.css fuera de zona, ver _BUZON.md) | 765cafc |
| SI-ayuda-html-tercer-pase | agente-si-ayuda-3 | 2026-09-06 | terminado (26 tickets verificados ticket por ticket contra el código actual: SI-160/161/162/164/165, VI-027/VI-073 ya estaban resueltos de verdad por pases previos, salvo un bug real sin ficha propia dentro de SI-165 -ver commit-; SD-161 y BR-180/OR-278/DG-165/SI-160 siguen bloqueados porque las secciones «Primeros Pasos»/«Módulo RRHH»/«Administración» son placeholders intencionales -"Próximamente"- a la espera de contenido que otros carriles deben enlazar aquí, no un bug de este archivo; SI-158/159/SD-207/VI-054 -sin cáscara ni `data-page`- confirmados intencionales por CLAUDE.md -página pública sin control de acceso propio-, ya documentado por SD-app-shell-tercer-pase; SI-163/SD-092/SD-210 -colores literales y bordes discontinuos- bloqueados por `styles.css` fuera de zona `[CHOCA]`; backoffice-rrhh.md/buscador-rrhh.md -enlace de ayuda contextual desde `hr.html`/`admin_hr.html`- y digitalizacion-escaner.md DG-159/165 -guía del escáner, `scanner-app/ui/` aún no existe- bloqueados por archivos ajenos, ya documentados. Bug real arreglado: en el toggle del FAQ, `aria-controls` sólo se fijaba si `body.id` ya existía, pero ningún `.help-faq-body` tenía `id` en el HTML -así que nunca se disparaba pese a estar marcado resuelto por SWEEP-misc-paginas-; se generan ids para botón y cuerpo antes de leerlos y se añade `aria-labelledby`, que el propio comentario del código prometía sin cumplir. Sin funciones referenciadas sin definir ni duplicadas frente al resto de `app/static/` -sin handlers externos en este archivo, verificado con grep-. `python -m pytest app/tests -q`: 846 passed antes y después. `python -m pytest app/tests/test_visual.py -q`: 54 passed) | eb96dec |
| SD-sistema-html-tercer-pase | agente-sd-sistema-3 | 2026-09-06 | terminado (sin cambios de código: los dos únicos tickets que citan `sistema.html` -SD-223 galería, SD-224 pruebas visuales- ya estaban resueltos por el carril LG y por LT (`test_visual.py`); el pendiente de LG "enlace de menú lateral" ya lo cerró agente-sweep-shell-core en `_BUZON.md` -añadió `/sistema` a `SHELL_SECCIONES`-. Revisión propia del archivo contra las cinco reglas de oro: sin colores/tamaños/duraciones a pelo -todo `var(--token)`-, sin funciones referenciadas sin definir -`escHtml` en `app.js`, `gsToken`/`gsRender*` sólo definidas aquí, sin duplicado en `app/static/js/` verificado por grep-, la insignia de "Recuerda usar var()" viva en modo oscuro porque los tokens que consume ya están redefinidos bajo `body.dark-mode` en `styles.css`. Sin ficha propia sin resolver dentro de mi zona. `python -m pytest app/tests -q`: 846 passed) | (sin commit, ningún archivo modificado) |
| SI-compartido-html-tercer-pase | agente-si-compartido-3 | 2026-09-06 | terminado (SI-169 arreglado de verdad: `AbortSignal.timeout(15000)` en el fetch más un aviso con botón "Reintentar" para timeout/errores del servidor; SI-170 arreglado: `<h1>` fijo tras la cabecera, presente en los tres estados -antes solo aparecía en el de éxito-, `<main>` pasa a `id="contenido-principal" tabindex="-1"` -el título del documento baja a `<h2>` para no duplicar el `<h1>`, `_marcarContenidoPrincipal()` de `app-shell.js` no corre aquí porque esta página no carga esa cáscara a propósito-; SI-015 arreglado en la única CDN de este archivo -FontAwesome 6.4.0-, con `integrity`/`crossorigin` calculados localmente con `openssl dgst -sha384` contra el archivo real descargado -no copiado de una tabla de terceros-, mismo método que usaron los pases previos de admin_ai/admin_system/ayuda; `fonts.googleapis.com` sin SRI porque su contenido varía por user-agent, igual que en el resto de páginas. SI-167 parcial: la caducidad del enlace ya se mostraba -VI-072, pase anterior-, añadido un enlace al sitio de la Facultad en el pie; el "emisor" sigue bloqueado porque `generate_share_token`/`verify_share_token` en `core/security.py` no llevan el usuario dentro del payload firmado -sólo módulo, id, caducidad y jti-, añadirlo es un cambio de formato de token fuera de mi zona (`compartido.html` no puede inventar un dato que la API nunca envía). SI-166 confirmado ya resuelto por `IN-share-revocation`: `descargar_compartido` en `share.py` genera la URL prefirmada de R2 directamente, sin pasar por `/api/files`. SI-168 (modo oscuro por `prefers-color-scheme`) sigue bloqueado, marcado `[CHOCA]` con `styles.css` en su propia ficha, fuera de zona. SI-123/SI-220/SI-236 no accionables sólo con este archivo -burbuja del asistente es decisión de producto, `robots.txt` va en `app/routes/pages.py`, guarda de `test_paginas.py` es archivo de test ajeno-. Sin funciones huérfanas: `esc`/`aviso`/`formatearCaducidad`/`fila`/`cargar` sólo existen en este archivo, verificado con grep en todo `app/static/`; sin definiciones duplicadas de las mismas funciones en otro archivo que cargue en esta página -sólo carga `app-theme.js`, sin colisión de nombres-. `node --check` OK sobre el script inline. `python -m pytest app/tests -q`: 846 passed antes y después. `python -m pytest app/tests/test_visual.py -q`: 54 passed) | ca3f85a |
| SD-investigacion-html-tercer-pase | agente-sd-investigacion-3 | 2026-09-06 | terminado (34+ tickets que citan `investigacion.html` en recorrido-visual.md/requisitos-vs-implementado.md/sistema-diseno.md/sistema-ia-paginas.md/PLAN-PARALELO.md verificados uno por uno contra el codigo actual: SD-207/SI-157 -Bootstrap 5.3 sin `styles.css`-, SI-156/SI-158 -pagina publica sin proteccion- y VI-056 -tablas a 9px, solo oscuro- ya estaban resueltos de verdad por pases previos -7c78070, c54c29c- sin que las tablas de auditoria lo reflejaran: hoy carga Bootstrap 4.6 vía `styles.css` -capa `@layer` de SD-187-, la ruta exige sesion + rol Global -`test_autorizacion_fixture.py`-, y la tabla ya usa 0.85rem envuelta en `.table-responsive` con scroll. SI-159/VI-054/SD-210 -sin cascara/app-shell, bloques `<style>` propios- confirmados intencionales por CLAUDE.md -pagina fuera del sistema de diseno a proposito-, igual que documentaron SD-app-shell-tercer-pase y SI-ayuda-html-tercer-pase. Bug real arreglado: SI-015 -6 dependencias CDN sin `integrity`/`crossorigin`- con SRI calculado localmente vía `openssl dgst -sha384` contra cada archivo real -mismo metodo que admin_ai/admin_system/ayuda/compartido-, fijando ademas las URLs de unpkg.com a la version exacta resuelta hoy -tippy.js@6.3.7, @popperjs/core@2.11.8- para que un patch nuevo no rompa el hash en silencio. Sin funciones referenciadas sin definir ni duplicadas frente al resto de `app/static/`. `python -m pytest app/tests -q`: 846 passed antes y despues. `python -m pytest app/tests/test_visual.py -q`: 54 passed) | a58e0a3 |
| BR-hr-py-tercer-pase | agente-br-hrpy-3 | 2026-09-06 | terminado | 7f16307 |
| OA-admin-docs-py-tercer-pase | agente-oa-docspy-3 | 2026-09-06 | terminado (OA-004/OA-005/OR-007/OR-031/OR-032/OR-100 arreglados de verdad; DG-139, OA-063, OA-103/OR-117, OR-257 y el resto de menciones a docs.py en backoffice-archivo.md/backoffice-rrhh.md/_BUZON.md verificados y descartados por depender de archivos fuera de zona -ver detalle en el commit-; 11 tests nuevos en test_admin_docs_pass3.py; pytest 846->860, 0 fallos) | 624e1be |
| BA-archive-py-tercer-pase | agente-ba-archivepy-3 | 2026-09-06 | terminado (BA-021/122/180/181/185 arreglados de verdad; BA-005/015/016/026/027/161/167 verificados ya resueltos por PASS2-archive-depth sin tocar de nuevo; resto bloqueado por archive.js/archive.html/pages.py/admin/docs.py/models.py/main.py/lookups.py/app-choices.js/utils.py fuera de zona o decision de producto pendiente en A2-archivo-backend, ver _BUZON.md. Sin bug sin ficha propia nuevo: el patron de deleted_at/columna derivada de hr.py y admin/docs.py no se repite en archive.py) | 27a6532 |
| OA-admin-imports-py-tercer-pase | agente-oa-importspy-3 | 2026-09-06 | terminado (arreglados de verdad: OR-008 -tipo RRHH nuevo caía en categoría Archivo por faltar cat_slug-, OR-106 -delimitador `;` de Excel en español se leía como una sola columna, ahora csv.Sniffer y `delimitador_detectado` en la respuesta-, OR-107 -cabeceras normalizadas sin acentos/mayúsculas/espacios + sinónimos ci/cedula_identidad/nombre/apellido-, OR-108 -`columnas_desconocidas`/`columnas_faltantes` en la respuesta en vez de ignorar en silencio-, OR-111 -tope 10 MB y 20000 filas por importación-, OR-113 -una cédula sólo en papelera ya no se revive en silencio al "actualizarla", se reporta en `en_papelera`-, IN-026 -CSV de sólo cédulas ya no crea empleados con nombres/apellidos en blanco, NOT NULL que "" satisfacía sin aviso-; el INSERT de documento+palabras clave de Archivo ahora corre en una sola `db_transaction` en vez de tres commits sueltos. Verificados ya resueltos sin regresión (carril fundacional C5-importaciones): OR-002/OR-003 -fecha_ingreso/titulo RRHH NOT NULL omitidas-, OR-004 -UPDATE que borraba nombres/apellidos/rif/fechas si el CSV no las traía-, OR-017 -rif vacío violando UNIQUE-, IN-048 -dos sentencias en una línea-. OR-110 -alerta de éxito con 0 insertados- ya resuelto en `admin-charts.js` por OA-admin-charts-tercer-pase, confirmado consistente con los campos que devuelve este archivo. Bloqueados fuera de zona (documentados, no tocados): OR-016/IN-027 -normalizar cédula- exige coordinar con `admin/docs.py` y `utils.py` porque las filas existentes en BD no están normalizadas y normalizar sólo aquí crearía altas duplicadas en vez de fusionar; OR-101/102/103/109(parcial)/112/114/115/116, OA-022/023, IN-024/047/048(resto)/054/109/133/164, RQ-014/028 exigen `admin-charts.js`/`admin/helpers.py`/`admin/docs.py`/`database.py`/`models.py`/`main.py` a la vez, marcados `[CHOCA]` en su propia ficha. `python -m pytest app/tests -q`: 860 passed antes y después, sin regresión) | b1fce79 |
| SI-core-ai-py-tercer-pase | agente-si-coreai-3 | 2026-09-06 | terminado parcialmente, rescatado tras corte de red (ENOTFOUND): SI-092 (interruptor en `ia_config` con env como respaldo), SI-063/IN-110 (timeout de `_post()` 90s→25s, coherente con `maxDuration` de Vercel), SI-085/IN-111 (catálogo de modelos persistido en `ia_config`, no solo en memoria de proceso) arreglados y verificados (sintaxis, pyflakes, pytest 860/860) antes de comitear. El agente murió justo tras decir "implementando los arreglos", así que puede haber más tickets de `core/ai.py` sin revisar — relanzar si hace falta profundizar (verificar primero qué queda contra `sistema-ia-paginas.md`/`_BUZON.md`) | e4a4401 |
| SI-routes-ai-py-tercer-pase | agente-si-routesai-3 | 2026-09-06 | terminado | eb0450b |
| OA-admin-stats-py-tercer-pase | agente-oa-statspy-3 | 2026-09-06 | terminado | ad782bc (ver nota en _BUZON.md: seis tickets reales arreglados solo en stats.py; el resto de menciones a stats.py exigian tocar archive.py/hr.py/retention.py/admin-charts.js/admin-stats.js/schema.sql y quedaron fuera de esta zona) |
| OA-admin-catalog-py-tercer-pase | agente-oa-catalogpy-3 | 2026-09-06 | terminado: desbloqueados varios tickets que el tercer pase de `admin-categories.js` dejó pendientes por depender de `catalog.py` — OA-124/OR-041 (columna `descripcion`, migración aditiva), OA-125/OR-040 (409 honesto con la categoría donde ya existe el nombre; la unicidad real por (nombre,categoría) exigiría `DROP CONSTRAINT` sobre el `UNIQUE(nombre)` de `schema.sql`, prohibido por las reglas de migración — documentado como bloqueado de verdad, no evadido), OA-123/OR-163 (nuevos `PUT`/`DELETE /admin/categories/{tid}`: renombrar, editar descripción, mover de Parte, fusionar reasignando `datos_archivo`/`datos_rrhh`, o desactivar con columna `activo` en vez de fallar con 500 por el FK RESTRICT al estar en uso — reutiliza `CategoryCreateRequest` para no tocar `models.py`), OA-130/OA-131 (fusión real de palabras clave vía `merge_into` en `DELETE /keywords/{kid}`). Bug sin ficha propia arreglado: `create_keyword`/`update_keyword`/`delete_keyword` no dejaban ningún rastro en `audit_log` (IN-083); `add_category` registraba "creado" incluso cuando la petición fallaba por nombre vacío o duplicado, ahora el log es posterior al INSERT real y usa el usuario de sesión en vez de `req.usuario` (IN-133). Fuga de datos personales real corregida: `get_audit_log` no filtraba por módulo — un admin de Archivo veía el detalle de auditoría de RRHH (nombres de empleados) y viceversa; ahora sólo un admin "Global" ve todo (OA-152), con filtros combinables por módulo/resultado/fecha ya en el backend (OA-151, la UI queda en `admin-ui.js`, fuera de zona). `get_keywords` acepta `search` opcional y una cota `LIMIT 5000` de seguridad sin cambiar el comportamiento actual (IN-096/IN-097). Migración añadida: índice `audit_log(timestamp DESC)` (IN-080 parcial — el índice trigram para `ILIKE` sobre `accion`/`usuario` queda pendiente de confirmar el privilegio `CREATE EXTENSION pg_trgm` en Neon, no se aplicó a ciegas). Quedan fuera de zona por exigir otros archivos: OA-128 (maquetación `admin_archive.html`), OR-042 (`admin.js` pisa Retención), OA-048 (sellado de auditoría, `database.py`+`main.py`, esfuerzo L), OR-265 (notificaciones por evento, esfuerzo L, nuevas rutas), OA-032 (índice trigram + debounce, mitad hecha aquí). `python -m pytest app/tests -q`: 806 passed (excluyendo `test_visual.py`, que falló por timeouts de Playwright preexistentes y no relacionados, confirmados en una corrida previa idéntica sin mis cambios) | c3c7b38 |
| OA-admin-users-py-tercer-pase | agente-oa-userspy-3 | 2026-09-06 | terminado parcialmente (OA-039/IN-142/IN-133 cerrados en `users.py`; caducidad, `debe_cambiar_password`, IN-141/IN-068/IN-079/OR-207 y el resto de IN-133 en otros archivos quedan `[CHOCA]`, ver `_BUZON.md`) | 05ad041 |
| OA-admin-retention-py-tercer-pase | agente-oa-retentionpy-3 | 2026-09-06 | terminado (OA-002/OA-003 real: `get_expired_docs` no respetaba `fecha_vencimiento` explícito, corregido; OR-183/OR-184: nuevo `GET /retencion/vencimientos-rrhh` sobre `datos_rrhh`, falta que `admin.js` lo llame; OA-144 parcial: constante `DEFAULT_PLAZO_ANIOS`; resto confirmado ya resuelto o bloqueado por `admin.js`/`main.py`/`storage.py` ajenos, ver `_BUZON.md`) | 34b9432 |
| OR-183-184-frontend-wiring | agente-or183-wiring | 2026-09-06 | terminado (OR-183/OR-184 cerrado del todo: `loadVencimientosTable()` en `admin/index.js` ahora llama a `GET /api/admin/retencion/vencimientos-rrhh` para el módulo RRHH en vez de mostrar el aviso de "no disponible", pintando la tabla con el mismo patrón visual que Archivo — icono/etiqueta de urgencia, estado vacío, error — más empleado y cédula del expediente. `admin_hr.html` ya tenía el marcado e ids correctos (`vencimientos-table-body-rrhh`/`-summary-rrhh`), no hizo falta tocarlo. Columna "Disposición" en "—": el endpoint es de sólo lectura, sin disponer sobre `datos_rrhh` todavía (OR-188). `node --check` OK. `python -m pytest app/tests -q`: 866 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed) | 73ec720 |
| C9-auth-py-tercer-pase | agente-authpy-3 | 2026-09-06 | terminado (IN-139, SI-021, SI-145 parcial en logout; resto documentado como bloqueado por zona/riesgo en _BUZON.md) | e59a0f6 |
| C10-files-py-tercer-pase | agente-filespy-3 | 2026-09-06 | terminado (bug real sin ficha propia arreglado: `GET /api/files/{key}` nunca consultaba `datos_archivo`/`datos_rrhh` en absoluto -sólo comprobaba módulo por prefijo de clave, OA-042-, así que un documento enviado a la papelera seguía siendo descargable por cualquiera con módulo correcto que tuviera el enlace viejo; `share.py` ya resolvió el mismo problema para enlaces externos -IN-147- y aquí no se replicaba. Nueva `_documento_en_papelera()`: sólo bloquea si la clave es el `file_url` **vigente** de un documento con `deleted_at IS NOT NULL`; si no coincide con ningún documento actual -versión histórica en `documento_versiones`, foto de empleado, clave huérfana- deja pasar, igual que `_modulo_permite_clave` con prefijo desconocido -fallar cerrado ahí rompería enlaces válidos sin evidencia-. 404 genérico, mismo patrón que `share.py`, no 403 -no confirma si la clave existe-. OA-047 (mitad de la ficha que vive en este archivo, el resto pide `admin/docs.py` `[CHOCA]`): cada descarga servida ahora llama `log_event` -en segundo plano, falla en silencio, sin latencia-, antes sólo se auditaba la subida. OA-042/DG-083 (identidad real, control por módulo) verificados ya resueltos por el carril fundacional C10-ficheros-r2, sin cambio necesario. Bloqueados sin tocar, documentados: DG-081 (subida huérfana en R2 al cancelar, exige `storage.py`+`admin-submit.js`+`admin-edit.js`), OA-011 (200MB anunciado vs 25MB real, exige `admin_archive.html`), OA-204 (SHA-256/PRONOM, esfuerzo L, exige `storage.py`+`main.py`+`backup.py`+`admin_archive.html`), SI-078 (adjunto de IA leído dos veces en memoria, exige `routes/ai.py` `[CHOCA]`), BR-008/BA-020 (`_secureFileUrl` con `?u=` decorativo, ya no hace nada real desde que `files.py` lo ignora — el arreglo pendiente es sólo cosmético en `app-core.js`/`hr.js`, fuera de zona), IN-124/IN-159/IN-160/IN-163/IN-171 (coste/control de acceso de R2, exige `backup.py`+`share.py` a la vez). `python -m pytest app/tests -q`: 866 passed antes, 877 passed después -11 tests nuevos en `test_serve_file.py`-; único fallo visto en la corrida completa fue `test_visual.py::rrhh-390px-oscuro`, confirmado flaky de Playwright ajeno a este archivo -pasa solo al repetirlo-) | cd325dd |
| C7-trash-py-tercer-pase | agente-trashpy-3 | 2026-09-06 | terminado: retomado tras el corte de sesión anterior (959a0c2, ver nota rescatada arriba) y completado. Añadida cobertura dedicada en `test_autorizacion_papelera.py` para OR-012 (caso 409 por documentos vivos, más el ajuste de mock que el conteo intermedio necesitaba). Revisé ticket por ticket todas las menciones a `trash.py` en `docs/auditoria/*.md`: mismo patrón de `deleted_at` ignorado que apareció en `hr.py`/`admin/docs.py`/`files.py` esta ronda, confirmado también aquí en `purge_employee` (OR-012) — arreglado con 409 en vez de dejar que el `ON DELETE CASCADE` de `datos_rrhh.empleado_id` (`main.py`, fuera de zona) arrastrara documentos vivos al purgar el expediente. OA-006/OA-007/OA-008/OR-009 ya resueltos (el commit 959a0c2 incluye el fix de OR-009: columna `empleado` propia en la papelera de RRHH, antes duplicaba `titulo`/`doc_type`). Bloqueados por archivo ajeno, documentados sin tocar: OR-010/OR-011 (cascada de borrado/restauración de empleado a sus documentos, exige `admin/docs.py` **[CHOCA]**), OA-133 (purga en lote, exige `admin-edit.js` **[CHOCA]**), OR-012 resto de la ficha (listar/confirmar con cédula en el modal, `admin-edit.js` **[CHOCA]**), OR-273 (retención/anonimización tras egreso, decisión de negocio + `retention.py`), IN-070/071/072 (esquema, `main.py` **[CHOCA]**), IN-084/164/165 (`database.py` **[CHOCA]**), IN-121 (`utils.py` **[CHOCA]**), IN-064 (reorganización de routers, `main.py` **[CHOCA]**), IN-049/IN-133 (identidad declarada por cliente / f-string de tabla, cambio de varios archivos a la vez). `python -m pytest app/tests -q`: 879/879 en verde tras el commit de tests. | 503014e |
| OR-hr-alerts-py-tercer-pase | agente-hralertspy-3 | 2026-09-06 | terminado (OR-155 parcial: add_position_history valida fecha_inicio contra fecha_ingreso/futuro/solape con tramo cerrado, mitad de cliente en admin-edit-hr.js fuera de zona. El patrón de deleted_at ignorado de esta ronda ya estaba resuelto aquí desde antes -commit 54237d8, PASS3 rrhh-backend-: jubilaciones filtra deleted_at, auditoría usa el actor de sesión, endpoint de fuga entre módulos ya retirado. Resto bloqueado por main.py/admin-edit-hr.js/admin/docs.py a la vez. python -m pytest app/tests -q: 879 passed antes y después) | 803fb98 |
| C8-backup-py-tercer-pase | agente-backuppy-3 | 2026-09-06 | terminado: revisadas todas las menciones a `backup.py` en `docs/auditoria/*.md`. Arreglado IN-157 (comparación de CRON_SECRET con `hmac.compare_digest` en vez de `!=`, y retirado `x_vercel_cron` que se declaraba y nunca se usaba) e IN-176 (la copia programada ya no se registra como éxito si alguna tabla falló al exportar: si fallan TODAS no se sube a R2 ni se registra, 502; si fallan algunas se sigue subiendo -no perder el resto- pero queda anotado en `backup_history` y logueado a nivel error; se compara además `total_rows` contra la última copia cron válida y se avisa, sin bloquear, ante una caída >50%). Confirmado ya resuelto sin tocar nada: SI-030 (validación de `_metadata.version` antes de tocar la base, por `agente-sweep-monitor-backup`) e IN-021/IN-022/O2 (transacción por tabla con rollback, reajuste de secuencia). Bloqueados por archivo ajeno, documentados sin tocar: IN-063 (mover el router de cron a `/api/cron/...`, exige `main.py` + `vercel.json` **[CHOCA]**), RQ-018 (segundo destino de respaldo + rotación de generaciones, exige `vercel.json` + `.env.example` **[CHOCA]**, y decidir proveedor/credenciales — no lo intento a medias), SI-138 (exportación sin barra de progreso, es sobre todo `admin_system.html`; sin cambio de streaming en el backend no hay bytes que contar en el cliente, y no hay ticket de frontend abierto para acompañarlo). `python -m pytest app/tests -q`: 877 passed, 6 fallos preexistentes en `test_visual.py` (`admin/archivo`, `admin/ia`, `ayuda`) sin relación con `backup.py`. | c655a3e |
| OA-lookups-py-tercer-pase | agente-lookupspy-3 | 2026-09-06 | terminado: `lookups.py` no tenía carril propio hasta ahora. Revisadas todas las menciones a `lookups.py` en `docs/auditoria/*.md`. Confirmado ya resuelto y correcto en el código actual (commit H1e `9a3a2e3`): BA-101/BA-145/BA-198 (`/api/choices` exige sesión y segmenta `archivo`/`rrhh` por módulo, `?scope=`), BA-163/IN-029 (sin `pandas`, `SELECT DISTINCT`/`MIN`/`MAX` directos), BA-165 (`TTLCache.invalidate()` borra valor y marca de tiempo juntas), IN-054 (ya no hay imports perezosos dentro de funciones). `invalidate_choices_cache()` sí se llama donde corresponde tras tocar `categoria`/`tipo_documento` (`admin/catalog.py`, `admin/docs.py`, `admin/retention.py`, `core/ai_proposals.py`). Arreglado de verdad, sin ticket propio en la auditoría: `catalogo.retencion` en `/api/choices` no estaba segmentado por módulo como sí lo están `archivo`/`rrhh` — un usuario sólo de Archivo recibía los `tipo_documento` de las 4 Partes de RRHH (y viceversa); ahora se filtra por `cat_slug` contra los mismos módulos/`scope`. Bloqueados por archivo ajeno `[CHOCA]`, documentados sin tocar: BA-004 (filtro por `tesauro_primario` en vez de FK real, `archive.py`), BA-046 (columna icono/color en `tipo_documento`, migración en `main.py`), BA-164/IN-028 (caché por proceso en serverless, necesita versión de esquema en `main.py`), BA-185 (función muerta `fetch_archive_dataframe`, vive en `archive.py`), IN-058 (mover `split_terms` de `database.py` a `utils.py`, rompe imports de `archive.py`/`hr.py`). `python -m pytest app/tests -q`: verde antes y después del cambio. | 4355060 |
| OA-admin-deps-py-tercer-pase | agente-adminddeps-3 | 2026-09-07 | terminado (sin cambios de código: revisadas todas las menciones a `deps.py` en `docs/auditoria/*.md` y en `_BUZON.md`, más `git log` completo del archivo -sólo `9ff1fc1`, `H1c-autorizacion`-. `require_role`/`require_admin_role` verificados línea a línea contra el código actual: ambos encadenan `Depends(require_session)`, consultan `_fila_usuario` con SQL parametrizado, fallan cerrado -403- si no hay fila, `is_active=FALSE`, o el módulo no es el permitido ni "Global"; "Global" es comodín correcto -sólo se salta el chequeo de módulo, nunca el de `is_active` ni el de rol en `require_admin_role`-; no encontré ningún parámetro ni cabecera con el que saltarse la comprobación. IN-131 -mecanismo de autorización real- está resuelto en este archivo desde H1c; su aplicación endpoint por endpoint es de cada router, no de aquí. Todo lo demás que sigue mencionando `deps.py` exige coordinar con archivo(s) fuera de mi zona declarada -sólo `app/routes/admin/deps.py`-, así que lo documento sin tocar en vez de arriesgar: SI-041 -`require_session` debería comprobar `is_active` contra la base- pide explícitamente caché en `core/cache.py` además de `deps.py`; implementarlo sin caché añadiría una consulta a Neon en CADA petición autenticada de la app entera -no sólo las que ya llevan `require_role`-, que es justo lo que la propia ficha pide evitar, así que no lo hago a medias. SI-047 -unificar resolución de "Global" entre `auth.py`/`deps.py`/`ai.py`- ya se intentó y se revirtió hoy desde el lado de `ai.py` -ver nota de `agente-*` en `_BUZON.md`- precisamente por cruzar los tres archivos; confirmo que sigue bloqueado igual desde `deps.py`. IN-143 -id de sesión revocable- y IN-155 -modelo de amenaza CSRF- necesitan `core/security.py`/`main.py`/`auth.py` a la vez. OR-207/SI-147 -roles intermedios y permisos finos- son cambio de esquema -`schema.sql`+`main.py`-. IN-150 -autorización de `/api/files` por documento, no por clave- es trabajo de `files.py`; no añadí ganchos especulativos en `deps.py` sin que exista quien los consuma. No até ningún test nuevo por no haber cambio de código. `python -m pytest app/tests -q`: 883 passed, en verde antes de empezar -no hice cambios que pudieran romperlo-.) | (sin commit — sin cambios de código)|
| OA-admin-helpers-py-tercer-pase | agente-adminhelpers-3 | 2026-09-07 | terminado | 9adfaa0 |
| SI-core-security-py-tercer-pase | agente-coresecpy-3 | 2026-09-07 | terminado (sin cambios de código): confirmado ya resueltos IN-145/IN-146 (claves derivadas, revocación por `jti`, commit `9dc3300`) e IN-056 (unificación de `hash_password`/`verify_password`, commit `11cc812`). Investigado IN-144 (usuario con `:` rompiendo `rsplit`) y no reproduce -`rsplit(":", 2)` es correcto-. Todo lo demás que menciona `security.py` -SI-042 caducidad absoluta de sesión, SI-040/SI-056/IN-143 token_version revocable, SI-047 resolución compartida de "Global", IN-155 CSRF, IN-039 chequeo de usuario activo, OA-039/IN-142 caducidad de contraseña, RQ-006/RF-003 invalidación real al cerrar sesión, SI-039 2FA- exige un cambio de formato de token (arriesga invalidar toda sesión activa) coordinado con `auth.py`/`admin/deps.py`, o un cambio de esquema en `main.py`/`schema.sql`; ninguno seguro de hacer en solitario dentro de esta zona, documentado sin tocar por prudencia explícita. `python -m pytest app/tests -q` en verde antes de empezar, sin cambios que pudieran romperlo | (sin commit — sin cambios de código) |
| OA-models-py-tercer-pase | agente-modelspy-3 | 2026-09-07 | terminado: primer carril dedicado a `app/models.py`. Revisadas todas las menciones a `models.py` en `docs/auditoria/*.md` y en `_BUZON.md` (incluidas las notas de `agente-h1d-modelos`, `agente-pass2-models-schema` y los pases de hoy sobre `admin/users.py`/`admin/catalog.py`). Arreglado de verdad: **OA-039/IN-142/SI-036** — `UserCreateRequest.password`/`PasswordChangeRequest.new_password` exigían sólo 6 como piso de Pydantic, contradiciendo el mínimo real de 12 que ya aplica `_validar_fuerza_password` en `admin/users.py` (6-11 caracteres pasaban la validación de forma y el endpoint los rechazaba igual con 400 — doble validación inconsistente); ahora ambos exigen 12 en el modelo también. **IN-133** — `UserCreateRequest.creator` y `PasswordChangeRequest.requester` eran campos de actor declarados por el cliente que el modelo seguía exigiendo como obligatorios pese a que `admin/users.py` ya usa el actor de la sesión (`require_session`) para auditoría desde hace varios pases; se retiraron ambos campos (Pydantic ignora extra por defecto, así que el frontend puede seguir mandándolos sin romperse). **IN-149** — `_validate_file_url` sólo exigía empezar por `/` o `https://`; un `file_url` con `..` (recorrido de ruta) pasaba, aunque `files.py:146` ya rechaza `".." in key` al servir; ahora `_validate_file_url` también lo rechaza. Confirmado ya resuelto sin tocar nada: **OA-124/OR-041** (`descripcion` de `tipo_documento`) — `CategoryCreateRequest.desc` se mapea correctamente en `admin/catalog.py` (`descripcion = (req.desc or "").strip() or None`), nada que arreglar del lado del modelo. Bloqueados por archivo ajeno `[CHOCA]`, documentados sin tocar: **RQ-014**/resto de `usuario`/`requester`/`creator` en `admin/docs.py`, `trash.py`, `admin/retention.py`, `admin/imports.py`, `files.py`, `hr_alerts.py` — siguen leyendo el actor del payload del cliente en vez de la sesión; retirar esos campos de los modelos correspondientes (`DocumentSubmitRequest.usuario`, `DocumentUpdateRequest.usuario`, `EmpleadoUpdateRequest.usuario`, `DocumentStatusUpdateRequest.usuario`, y el `requester` propio de `retention.py`) rompería esas rutas hasta que cada una deje de leerlos — necesita carril grande coordinado, no lo hago a medias. **RQ-009** (`RrhhSearchRequest.people_terms` código muerto) sigue bloqueado: `hr.py:169-172` todavía lo lee. **IN-051** (`extra="forbid"` ausente en todos los modelos), **IN-052** (fechas como `str` + regex en vez de `datetime.date`), **IN-046** (`DocumentSubmitRequest` modela dos entidades a la vez) — cambios estructurales de alcance L que tocan `admin/docs.py`/`admin/imports.py`/frontend a la vez, documentados sin tocar por prudencia (arriesgarían las 883 pruebas y varias rutas activas sin poder coordinar con esos carriles en esta pasada). OR-084 (validación real de RIF), OR-085/OR-086/OR-142 (edición de más campos del expediente), IN-165 (idempotencia) — todos exigen `admin/docs.py`/`admin/imports.py`/`main.py` a la vez. `python -m pytest app/tests -q`: 883 passed antes y después. | 3847bdb |
| OA-storage-py-tercer-pase | agente-storagepy-3 | 2026-09-07 | terminado: primer carril dedicado a `app/storage.py`. Arreglado de verdad: IN-126 (retries/timeouts explícitos + `_client_lock` puesto en uso real con double-checked locking), IN-151 (clave de objeto sin el nombre original, sólo UUID completo + extensión), IN-148 (Content-Type fijado por el servidor según extensión validada, no el que declara el cliente), DG-080 (`.jp2` añadido a `ALLOWED_EXTENSIONS`). Confirmado ya resuelto de un pase anterior sin cambio: IN-106 (import perezoso de boto3). Bloqueados por archivo ajeno `[CHOCA]`, documentados en `_BUZON.md`: IN-117/118/119/124/125/149/159/160/163/171/190, DG-081/096/104/120/140/141/142/144/148/152/153. `python -m pytest app/tests -q`: 883/883 antes y después | 7e541fc |
| O2-database-py-tercer-pase | agente-databasepy-3 | 2026-09-07 | terminado sin cambios de código, por prudencia deliberada: tercer pase sobre el archivo de mayor radio de explosión del proyecto (incidente real hoy con `statement_timeout` vía `options` tumbando toda conexión a Neon en producción, invisible para pytest porque toda la suite mockea `db_query`). Revisé ticket por ticket todas las menciones a `database.py` en `_BUZON.md`, `_RESERVAS.md` e `ingenieria.md`/`backoffice-archivo.md`/`backoffice-rrhh.md`/`requisitos-vs-implementado.md`/`sistema-ia-paginas.md`/`digitalizacion-escaner.md`, más `git log --all -- app/database.py`. Confirmado ya resuelto por pases previos (H1b-conexion `9a3a2e3`, PASS2-engineering `f264057`, y hoy `4141182`/`11cc812`): IN-004 (rollback tras SELECT para no dejar la conexión "idle in transaction"), IN-107/IN-108 (`DB_POOL_MIN`/`DB_POOL_MAX` de `config.py` sí en uso), IN-164 (`db_transaction()` para escrituras compuestas), IN-166/IN-167 (reintento sólo en lecturas, sin `sleep` bajo `PoolError`, 503 rápido con `Retry-After`), IN-168 (`connect_timeout=3`), IN-169 (el statement_timeout va como `SET` de sesión, no como parámetro de arranque — la causa exacta del incidente de hoy, ya corregida), IN-032/IN-033 (`DATABASE_URL` con fuente única en `settings.database_url`), IN-058 (`split_terms` re-exportado desde `utils.py`), IN-056 (`hash_password`/`verify_password` re-exportados desde `core/security.py`, ya no hay implementación duplicada que no atrape la excepción de bcrypt). Verificado también: `%` sin duplicar en SQL dinámico NO se maneja aquí — depende de cada caller (cubierto por `test_sql_columns.py`), correcto por diseño porque `database.py` no construye SQL, sólo lo ejecuta. `getconn()`/`putconn()` están cubiertos con `finally` en las tres rutas (`db_query`, `db_transaction`, el `except PoolError`), sin fuga de conexión que yo haya encontrado bajo excepción a mitad de camino. Todo lo demás que sigue mencionando `database.py` en la auditoría (IN-042 capa de repositorios, IN-053 `@overload` en `db_query`, IN-080/081/082/083/084 reforma de `audit_log`, IN-100 réplica de lectura, IN-101 endpoint agrupado, IN-127/181/182/183/187/191 observabilidad y logging, IN-214 migración a psycopg3, RF-041/RF-042/F-10 actor de auditoría declarado por el cliente y `log_event` en hilo `daemon=False` que promete durabilidad que Vercel no garantiza, SI-207 pool por instancia) exige tocar `main.py`, `core/config.py`, `models.py`, o media docena de `routes/*` a la vez — marcado `[CHOCA]` en su propia ficha, o de alcance L/arquitectónico. Decidí no tocar ninguno en solitario: son exactamente el tipo de cambio que un tercer pase aislado no puede verificar con pytest (que mockea `db_query` para todo) y que ya causó el incidente de hoy cuando se hizo sin ese cuidado. `python -m pytest app/tests -q`: 883 passed, sin tocar código de `database.py`. Sin verificación post-despliegue pendiente porque no hay cambio que desplegar. | (sin commit de código; este registro es el único cambio) |
| H1a-main-py-tercer-pase | agente-mainpy-3 | 2026-09-07 | terminado sin cambios de código, por prudencia deliberada: tercer pase sobre `app/main.py`, señalado en el encargo como el segundo archivo de mayor radio de explosión del proyecto (justo detrás de `database.py`, que hizo su tercer pase hoy también con CERO cambios de código tras el incidente real de `statement_timeout`). `run_migrations()` corre en cada arranque en frío contra Neon en producción real; un error de sintaxis SQL en una migración nueva no lo detecta pytest (`test_migraciones.py` sólo compara la huella SHA-256, no ejecuta contra base real). Revisé íntegro `app/main.py` (896 líneas: ~70 migraciones idempotentes en `run_migrations()`, CORS, middlewares de caché/logging/arranque perezoso, `include_router`, `/api/health`) y crucé TODAS las menciones a `main.py` en `docs/auditoria/_BUZON.md` (tabla de pendientes: 81 referencias) y en `docs/auditoria/*.md`, más `git log --oneline --all -- app/main.py` (últimas migraciones de hoy: `descripcion`/`activo` en `tipo_documento` y `idx_audit_log_timestamp`, commit `c3c7b38` OA-catalogpy-3; `jti` de enlaces revocados `9dc3300`; CORS sin esperar a `config.py` `ada3784`). Confirmado ya resuelto, sin tocar nada: IN-113/IN-114 (política de caché de `/static/*.js`.css`.html` retirada de `add_no_cache_header`, ya sólo la fija `vercel.json`), IN-034 (CORS lee `ALLOWED_ORIGINS` del entorno sin esperar a `config.py`), IN-001 (arranque enganchado a la primera petición real vía `_startup_middleware` porque Mangum corre con `lifespan="off"` en Vercel), SI-031 (tabla `login_attempts` con índice único `usuario,ip` ya en `run_migrations()` — sólo falta el lado de `auth.py`, fuera de zona), OR-177/OR-179 (`deleted_reason` en las tres tablas), IN-145/IN-146 (tabla `enlaces_revocados` por `jti`), OA-124/OR-041/OA-123/OR-163 (`descripcion`/`activo` en `tipo_documento`), IN-080 parcial (`idx_audit_log_timestamp` — el trigram para búsqueda por `accion`/`usuario` queda anotado en el propio código como pendiente por privilegio de extensión no verificable en frío). Ningún ticket pendiente resultó ser una migración aditiva trivial y aislada sin verificación cruzada: absolutamente todo lo que queda (IN-063 mover prefijo del cron exige `vercel.json` a la vez; IN-136 exige `core/config.py`; IN-138/SI-012/SI-013 rate limiting real exige Vercel Edge o `core/ai.py`; RQ-041/RQ-044/RQ-049/OR-072/OR-256 columnas nuevas que necesitan `models.py`+rutas para tener consumidor; DG-138/DG-139/OR-207/OR-277 features nuevas con HTML/JS coordinado; IN-068/IN-079/OA-039/IN-142 columnas+`auth.py`/`users.py` a la vez; IN-181-191 logging estructurado que exige `database.py`/`storage.py` a la vez; SI-082/SI-064 requieren `core/ai.py`+`admin_ai.html`) está marcado `[CHOCA]` en su propia ficha o exige coordinar con `vercel.json`/frontend/otro router — exactamente fuera de mi zona declarada (`app/main.py` en solitario). No até ningún bug de código Python sin SQL (imports, lógica de middleware) que estuviera claro y sin ficha propia. `python -m pytest app/tests -q`: 883 passed tanto al empezar como al terminar, sin cambios que pudieran romperlo. Sin verificación post-despliegue pendiente porque no hay cambio que desplegar. | (sin commit de código; este registro es el único cambio) |
| OA-utils-py-tercer-pase | agente-utilspy-3 | 2026-09-07 | terminado: primer carril dedicado a `app/utils.py`. Revisadas todas las menciones a `utils.py` en `docs/auditoria/*.md` y `git log --all -- app/utils.py` (`b0616ca` extrajo `paginate()`, `f264057` PASS2-engineering, e IN-058 de hoy que reexportó `split_terms` desde `database.py`). Arreglado de verdad: **IN-092** — `generate_unique_slug` reintentaba `SELECT` sin tope ante colisiones; ahora corta a los 50 intentos (`_MAX_SLUG_ATTEMPTS`) y cae a un sufijo `uuid4` corto que no depende de contar filas. Añadido: **IN-044** — `tiene_letras()` nueva (basada en `str.isalpha()` Unicode) lista para reemplazar el regex Latin-1 `[A-Za-zÀ-ÿ]` repetido en `archive.py`/`hr.py`/`admin/docs.py`; migrar esos call sites queda **[CHOCA]**, fuera de zona. Confirmado ya resuelto sin tocar nada: **BA-169** (offset topado con `max_offset`, ya en `paginate()`), **IN-057** (el `sanitize_filename` duplicado de `utils.py` ya no existe en el archivo — sólo queda la implementación real en `storage.py`), **IN-058** (`split_terms` ya vive aquí, reexportado desde `database.py` como registra la propia nota IN-058 en el docstring). Bloqueados por archivo ajeno `[CHOCA]`, documentados sin tocar: **IN-055** (separar `utils.py` puro de un `slugs.py` con acceso a datos — exige `main.py`/`admin/helpers.py` a la vez), **IN-091**/**IN-093** (arranque en frío N+1 de `populate_missing_slugs`/`_migrate_archivo_tipos` — exige `main.py`, y SI-208 ya lo señala como candidato a mover fuera del startup, no a reescribir aislado en `utils.py` sin coordinar), **IN-121**/**IN-122** (paginación por cursor y tope único de página — la propia ficha deja `paginate()` como está para el caso general y pide el cambio en los endpoints, `[CHOCA]`), **BR-072** (`split_terms` funciona bien; el bug real es en `hr.py:225` usando `first_tipo`, no en la función). No apto para esta pasada: IN-198 (mypy) es config de proyecto, no código de `utils.py`. `python -m pytest app/tests -q`: verde antes y después (dos corridas, exit 0 cada una). | 2b61ba4 |
| OA-share-py-tercer-pase | agente-sharepy-3 | 2026-09-07 | terminado: primer carril dedicado en solitario a `app/routes/share.py`. Leído completo `CLAUDE.md` (sección `share.py`), el archivo entero, `core/security.py`, `git log --oneline --all -- app/routes/share.py` (`9dc3300` IN-145/IN-146 hoy, `a3052c6` C10-ficheros-r2/DG-083/IN-147, `99a2a2a` versión original), y todas las menciones a `share.py`/`compartid` en `docs/auditoria/*.md` y `_BUZON.md`. Arreglado de verdad: **IN-133** — `crear_enlace` y `revocar_enlace` ya exigían sesión/rol admin vía `Depends`, pero además aceptaban un parámetro `usuario` de query string suelto que quedaba tal cual en `log_event`: cualquiera con sesión (o con rol admin de cualquiera de los dos módulos) podía firmar la creación o revocación de un enlace externo con el nombre de otra persona, y la auditoría lo habría dado por bueno — exactamente el patrón que IN-133 describe para todo el proyecto, aquí en la única puerta pública sin sesión. Ahora `usuario` sale de `Depends(require_session)`/`Depends(require_admin_role(...))`, no de un parámetro que el cliente rellena. **SI-025** — `leer_compartido`/`descargar_compartido` auditaban sólo `doc_id`: si un enlace se filtraba y se consultaba muchas veces no había forma de distinguir una consulta de otra ni de saber si fue una persona o cuarenta. Añadido `_detalle_acceso()`: registra también `jti` (cruzable con "Crear Enlace Externo", que ya lo guardaba), IP y user-agent del `Request`, con `"?"` de reserva si algo falta. Verificado ya resuelto sin tocar nada: **IN-147** (`_leer_documento` ya exige `status='aprobado'` además de `deleted_at IS NULL`, commit `a3052c6`), **IN-145/IN-146** (clave derivada por propósito + revocación por `jti`, `core/security.py`, commit `9dc3300` — la caducidad y la revocación se comprueban dentro de `verify_share_token`, que es el único camino que usan los tres endpoints públicos: crear, consultar y descargar comparten la misma comprobación, no hay ninguno que se salte la revocación), **BA-019/SI-166** (`descargar_compartido` genera URL prefirmada de R2 directamente, no pasa por `/api/files`). Bloqueados por archivo ajeno, documentados sin tocar: **IN-049** (f-string en `_leer_documento` para tabla/columna — de bajo riesgo real porque `modulo` ya está validado contra el diccionario `_TABLAS` antes de usarse, pero el arreglo con `psycopg2.sql.Identifier` es un patrón que la ficha marca para aplicar a la vez en `admin/docs.py`/`trash.py`/`backup.py`/`admin/helpers.py` `[CHOCA]`, no lo hice aislado), **IN-118** (falta `Content-Disposition` en la redirección — exige `storage.py` `[CHOCA]`), **IN-124** (coste de egreso sin tope — exige `files.py`+CDN `[CHOCA]`), **IN-138** (rate limiting — exige `vercel.json`/`main.py` `[CHOCA]`). No encontré ningún bug nuevo sin ficha propia más allá de IN-133/SI-025. `python -m pytest app/tests -q`: 883 passed antes de empezar y 883 passed después (una sola corrida completa tras el grupo de cambios, todos en el mismo archivo). | 5be765c |
| SI-core-ai-proposals-py-tercer-pase | agente-aipropospy-3 | 2026-09-07 | terminado: primer carril dedicado en solitario a `app/core/ai_proposals.py`. Leído el archivo completo, `git log --oneline --all -- app/core/ai_proposals.py` (`2f4de10`/`5779b02` renombres a inglés, `a3052c6` D1-ia-backend con SI-004/005/009/010/011/022/023), y todas las menciones al archivo en `docs/auditoria/*.md` y `_BUZON.md` (sistema-ia-paginas.md, requisitos-vs-implementado.md). Confirmado ya resuelto sin tocar nada, verificado contra el código actual, no sólo las notas: **SI-009** (`reject` exige el mismo módulo que `approve`), **SI-010** (`list_proposals` acota por `modulos`/`usuario`), **SI-022** (el `UPDATE ... WHERE estado='pendiente' RETURNING id` reclama la propuesta antes de ejecutar, en `approve` y en `reject`, sin ventana para doble aprobación), **SI-023** (`_actualizar` exige `deleted_at IS NULL` con `RETURNING`, y si el documento ya no está, `PropuestaError` explícito) — los cuatro con test en `app/tests/test_ia_seguridad.py`. Verificado también que `usuario` en `approve`/`reject` sale de `Depends(require_session)` en `app/routes/ai.py`, no de un parámetro que declare el cliente (mismo patrón IN-133 ya cerrado hoy en `share.py`; aquí no hacía falta arreglo porque ya estaba bien). Arreglado de verdad: **bug sin ficha propia** — `approve()` sólo revertía la propuesta a `pendiente` cuando la ejecución lanzaba `PropuestaError`; cualquier otro fallo (corte de conexión a mitad del `UPDATE`/`INSERT`, un dato con forma inesperada dentro de `_actualizar`/`_crear`/`_palabras_clave`) dejaba la fila marcada `aprobada` -con `resuelto_por`/`resuelto_at` ya escritos- sin que el cambio se hubiera aplicado de verdad: auditoría falsa (dice que alguien aprobó y aplicó un cambio que nunca ocurrió) y la propuesta queda fuera de alcance para reintentar o rechazar. Ampliado el `except` a `Exception`. Bloqueados por archivo ajeno, documentados sin tocar: **SI-107**/**SI-108** (aprobar/rechazar y ver antes/después desde `admin_ai.html` — exigen endpoint nuevo en `app/routes/ai.py` + `admin_ai.html`), **SI-024** (adjuntar por propuesta sobrescribe el digitalizado anterior sin versión — la propia ficha lo marca `[CHOCA]`, depende de OA-008 y de `app/routes/trash.py`), **SI-130** (el backup no incluye `ia_propuestas` — es `app/routes/backup.py`), **SI-228**/**SI-231** (tests nuevos del ciclo de propuestas — `app/tests/test_ia.py`, no es este archivo). No encontré ningún otro bug nuevo sin ficha propia. `python -m pytest app/tests -q`: 883 passed antes de mi cambio y 883 passed después (dos corridas completas en primer plano). | 9562873 |
| SI-core-ai-tools-py-tercer-pase | agente-aitoolspy-3 | 2026-09-07 | terminado: primer carril dedicado en solitario a `app/core/ai_tools.py`. Leído `CLAUDE.md`, el archivo completo, `git log --oneline --all -- app/core/ai_tools.py` (`c7b5995`/`5779b02`/`2f4de10` renombres a inglés; `a3052c6` y `20548d9` son commits de OTROS carriles que arrastraron el archivo por una carrera de git, ya documentados en `_BUZON.md`), y todas las menciones al archivo en `docs/auditoria/{ingenieria,requisitos-vs-implementado,sistema-ia-paginas}.md` y `_BUZON.md`. Confirmado ya resuelto sin tocar nada, verificado contra el código actual línea por línea, no sólo las notas: **SI-020** (`_search_archive`/`_get_document` ya filtran `status='aprobado'` para perfil `publico`), **SI-019** (`_navigate_to`/`_rutas_para` ya filtran `_RUTA_REQUIERE_MODULO` contra los módulos del contexto), **SI-095** (`_search_hr_document`, `_employee_file` y la rama RRHH de `_get_document` ya llevan `deleted_at IS NULL`) — los tres con test o comentario citando el número de ficha en el propio código. **IN-161** (las herramientas no comprobarían módulo al consultar): la mitad que toca este archivo ya está resuelta — `definitions()` filtra por `perfil`+`módulo` del contexto y `execute()` repite la comprobación antes de despachar (doble cerrojo documentado en el docstring, líneas 113-134); la parte pendiente del ticket (capa `services/` compartida con `routes/ai.py`, ligada a IN-131) queda bloqueada, cruza archivo. Arreglado de verdad: **SI-018** — los resultados de herramienta viajan a `ai.py::converse()` como turno `role: tool` con el contenido de documentos (abstract, notas, personas_relacionadas: texto libre cargado por cualquier admin de módulo) sin ninguna marca de que sea dato y no instrucción; un documento con "Instrucción: ignora las reglas anteriores" en el resumen se veía, para el modelo, igual de legítimo que el prompt real. `execute()` ahora añade `_advertencia` a todo resultado sin `error`, marcando el contenido como dato no confiable. Es defensa en profundidad (la principal sigue siendo que la herramienta no existe para quien no debe verla); el refuerzo correspondiente en el prompt de sistema (`ai_prompts.py`) queda fuera de esta zona, documentado como pendiente. Comprobado el patrón del bug real de `ai_proposals.py` de hoy (estado que no refleja la realidad tras un fallo a mitad de camino) y **no se repite aquí**: las cuatro herramientas de escritura (`_propose_update`, `_propose_document`, `_propose_attach_file`, `_propose_keywords`) sólo hacen un único `INSERT ... RETURNING id` atómico en `_create_proposal`, nunca marcan nada como "aplicado" — esa responsabilidad vive entera en `ai_proposals.py`, ya corregida por el carril anterior. Bloqueados por archivo ajeno, documentados sin tocar: **SI-230**/**SI-231**/**SI-228** (tests nuevos — `app/tests/test_ia.py`, no es este archivo), **SI-018** parte de `ai_prompts.py` (arriba). No encontré ningún otro bug nuevo sin ficha propia. `python -m pytest app/tests -q`: 883 passed antes y 883 passed después. | 4b2c62f |
| SI-core-ai-prompts-py-tercer-pase | agente-aipromptspy-3 | 2026-09-07 | terminado: primer carril dedicado en solitario a `app/core/ai_prompts.py`. Leído `CLAUDE.md`, el archivo completo, `git log --oneline --all -- app/core/ai_prompts.py` (sólo `2f4de10`, el renombre a inglés), y todas las menciones al archivo en `docs/auditoria/*.md` y `_BUZON.md` (sistema-ia-paginas.md: SI-018, SI-069, SI-090, SI-228). Arreglado de verdad: **SI-018** (parte pendiente que dejó documentada `agente-aitoolspy-3` en `4b2c62f`) — el prompt no explicaba que el contenido de una herramienta es dato y no instrucción, aunque `ai_tools.py::execute()` ya lo marca con `_advertencia`. Añadido bloque "SEGURIDAD" a `_BASE` (bloque estable, aplica a los tres perfiles): explica que título/abstract/notas/personas_relacionadas son texto libre cargado por cualquiera con permiso, que una instrucción con esa forma dentro de un resultado no se obedece, y que sólo el prompt de sistema y el mensaje de quien pregunta mandan. Verificado, sin tocar: **SI-069** (caché de prompt no estable porque `nombre_usuario` entra en el bloque que `ai.py::system_block()` marca entero con `cache_control`) — el arreglo que pide la ficha es partir en dos bloques (estable cacheado / variable sin marcar), lo que exige cambiar la firma de `prompt()` y cómo `ai.py::converse()` arma `conversacion` con `system_block()`: no se puede tocar sólo `ai_prompts.py` sin romper el contrato con `ai.py`, que es zona ajena. Bloqueado, documentado sin tocar. **SI-090** (regla de privacidad de RRHH es una frase suelta) — la propia ficha pide el arreglo real en la herramienta (`expediente_empleado` en `ai_tools.py` debe enmascarar cédula y omitir fecha de nacimiento/sexo si no se piden), no en el prompt: "recortar en la herramienta, no en el prompt". No hay nada que hacer aquí; documentado como verificado sin acción de prompt. **SI-228** (tests nuevos del módulo IA) — `app/tests/test_ia.py`, no es este archivo, bloqueado por archivo ajeno. No encontré ningún bug nuevo sin ficha propia en `ai_prompts.py`. `python -m pytest app/tests -q`: 883 passed antes y 883 passed después (una corrida completa en primer plano). | f18170d |
| IN-core-cache-config-py-tercer-pase | agente-cacheconfig-3 | 2026-09-07 | terminado: tercer pase sobre `app/core/cache.py` (`TTLCache`, sin condición de carrera real ni ticket accionable — `IN-028` sigue `[CHOCA]` con `lookups.py`/`main.py`, `BA-165` ya resuelto por `OA-lookups-py-tercer-pase`) y `app/core/config.py` (`IN-032`/`IN-033` ya resueltos por `PASS2-engineering`; `IN-136` confirmado genuinamente bloqueado — ningún test fija `ENVIRONMENT`/`SECRET_KEY`, un fail-closed en tiempo de importación tumbaría los 883 tests; `SI-219` descartado a propósito, riesgo de despliegue por ganancia cosmética). Arreglado sin ticket propio: `_int_env()` nombra la variable en el `ValueError` cuando `CHOICES_CACHE_TTL`/`DB_POOL_MIN`/`DB_POOL_MAX` vienen mal formadas. Detalle completo en `_BUZON.md`. `python -m pytest app/tests -q`: 883 passed antes y después. | 00c9b32 |
| F2-pages-py-tercer-pase | agente-pagespy-3 | 2026-09-07 | terminado (sin cambios de código: nada accionable en zona, ver nota en _BUZON.md) | be61849 |
| IN-133-admin-docs-py-followup | agente-in133-docspy | 2026-09-07 | terminado: cerrados los 7 sitios de `admin/docs.py` que usaban `req.usuario` (campo declarado por el propio cliente) confirmados por grep — `admin_submit` (log_event + `creado_por`), `update_documento` (`updated_by` + log_event Archivo + log_event RRHH) y `update_empleado` (`updated_by` + log_event) — reemplazados por `usuario_sesion`, ya disponible en la firma de los cinco endpoints vía `Depends(require_session)` sin necesidad de añadir nada. Mismo patrón que hoy en `admin/catalog.py`, `share.py` y `admin/users.py`. `DocumentSubmitRequest.usuario`/`DocumentUpdateRequest.usuario`/`EmpleadoUpdateRequest.usuario` siguen declarados en `models.py` (documentado como pendiente por `agente-modelspy-3`) pero ya no se leen en `docs.py`; retirarlos del modelo queda para otro carril, fuera de esta zona. `python -m pytest app/tests -q`: 883 passed antes; 881 passed + 2 failed después (`test_visual.py::admin/sistema-1440px-oscuro` y `sistema-768px-claro`, Playwright bajo carga completa — confirmado sin relación con este cambio ni con `docs.py`: ambos pasan en corrida aislada). | c2d96e7 |
| SI-228-230-231-test-ia | agente-testia-3 | 2026-09-07 | terminado: creado `app/tests/test_ia.py` (10 tests nuevos), revisado antes `test_ia_seguridad.py` para no duplicar patrón/casos. Cubre los bugs reales del barrido de hoy que quedaron sin test: el bug de integridad de auditoría de `ai_proposals.approve()` (revertir a 'pendiente' ante CUALQUIER excepción, no sólo `PropuestaError` — commit `9562873`; verificado el test falla si se revierte el `except` a sólo `PropuestaError`), SI-022 (aprobar dos veces no ejecuta el cambio real dos veces), SI-018 en `ai_tools.execute()` (`_advertencia` en éxito, no en error, `setdefault` no pisa una propia), y SI-121 en `routes/ai.py POST /chat` (`gasto_hoy`/`tope_diario` en cada respuesta, y que un fallo en ese cálculo final no tumbe la respuesta ya generada). `python -m pytest app/tests -q`: 896 passed (886 antes + 10 nuevos). | 77c328e |
| RECORRIDO-visual-funcional-2 | agente-recorrido2 | 2026-09-07 | terminado (documento en `recorrido-visual-2.md`, sin cambios de código: hallazgo crítico `apiFetch` trata 403 como 401 y cierra sesión válida, panel de personalización sin backdrop a 390px, Chart.js sin destruir instancia previa antes de re-render; hueco de fidelidad en el arnés `fakedb.py` -no fija la fila de `admin.global`- documentado, impidió probar en vivo Retención-RRHH y ciclo de propuestas de IA) | c1284d2 |
| BUG-403-cierra-sesion | agente-bug403 | 2026-09-07 | terminado | aa3402a |
| BUG-chartjs-canvas-reuse | agente-bugcharts | 2026-09-07 | terminado (rescatado tras corte de sesión, verificado sintaxis+pytest antes de comitear): `_destroyAnyChartOn(canvas)` con `Chart.getChart(canvas)` como red de seguridad antes de cada `new Chart(...)`, en los 8 sitios del archivo; guarda contra `it.text` vacío en `generateLabels` | 48feb6b |
| BUG-theme-panel-sin-backdrop | agente-bugthemepanel | 2026-09-07 | terminado: nuevo `#ds-theme-panel-overlay` en `app-theme.js` (creado/reusado en `openThemePanel()`, click cierra el panel) con CSS en `personalizacion.css` calcado de `.ds-sidebar-overlay`/`.ds-sidebar` (shell.css, BUG-menu-bugueando) — velo en `--z-overlay`, panel en `--z-modal` (antes `1060` a pelo). `python -m pytest app/tests -q`: 897 passed, 1 failed (`test_visual.py::admin/sistema-1440px-oscuro`, sin relación, página no tocada; aislado `test_visual.py`: 54 passed). | 65dc0e6 |
| ARNES-fakedb-admin-global | agente-arnesfix | 2026-09-07 | terminado, con corrección sobre el cierre anterior de esta misma fila: aquel cierre marcó el hallazgo #6 como "no bug" sólo por lectura de código, sin reproducirlo en vivo. Repetido con Playwright contra el servidor real y SÍ es un bug: en `app/static/js/admin/monitor.js` (~línea 476), el botón `.ds-status-btn` de la pestaña Documentos arma su `onclick` con una plantilla que inserta `JSON.stringify(f.status||'aprobado')` y `JSON.stringify(state.user.modulo)` sin escapar dentro de un atributo HTML entrecomillado con `"` — como `JSON.stringify` envuelve el string en `"..."`, cierra el atributo antes de tiempo. HTML resultante confirmado con `outerHTML`: `onclick="openQuickStatusMenu(this,1," activo","archivo")"=""`. El navegador no puede compilar ese `onclick` (`pageerror: Unexpected end of input`), así que el clic no dispara nada — ni modal ni menú — con cualquier fila cuyo estado no sea `undefined`. No lo arreglé (zona `app/` fuera de mi carril); necesita carril de arreglo aparte. El resto del cierre anterior (arreglo de `fakedb.py`, pruebas de Retención-RRHH y ciclo de propuestas IA) se reverificó y sigue en pie: `GET /api/admin/retencion/vencimientos-rrhh` 200/200/200/200/200, `GET /api/ia/propuestas` 200, `POST /api/ia/propuesta/{id}/aprobar\|rechazar` ya pasa la autorización (400, no 403/401 — rechaza por `estado` falso ≠ 'pendiente', limitación del generador de datos falsos, no bug real). | 40b7782 |
| IN-133-status-endpoint-followup | agente-luisdavid | 2026-09-07 | terminado: `PATCH /documento/{id}/status` (`admin/docs.py`) seguía mandando `requester` (query param del cliente) a `log_event` en vez de `usuario_sesion`, que ya llegaba por `Depends(require_session)` en la firma — mismo patrón IN-133 cerrado hoy en otros endpoints del archivo, que no cubrió este por separado. `python -m pytest app/tests -q`: 898 passed antes y después | 1c68a01 |
| BUG-onclick-json-sin-escapar | agente-luisdavid | 2026-09-07 | terminado: el mismo patrón roto de ARNES-fakedb-admin-global (`JSON.stringify` sin escapar dentro de un atributo `onclick="..."`) aparecía en 17 sitios de 6 archivos (`monitor.js`, `hr.js`, `admin/edit.js`, `admin/index.js`, `admin/categories.js`, `admin/users.js`), no solo el botón de estado. Arreglado uniformemente envolviendo cada `JSON.stringify(...)` con el `escHtml()` ya existente en `app.js`. `node --check` en los 6 archivos, `python -m pytest app/tests -q`: 898 passed, `test_visual.py`: 54 passed, y clic real vía Playwright contra el arnés local confirmando que `.ds-status-btn` ahora abre `.ds-quick-status-menu` sin errores de consola/página. | be07dff |
| SI-badge-sidebar-a11y | agente-siguiente-ronda | 2026-09-07 | terminado: hallazgo #5 de recorrido-visual-2.md, `app-shell.js` `_shellLink()` — badge de letra suelta ("A"/"R") ahora `aria-hidden="true"` por ser decorativo. `python -m pytest app/tests -q`: 898 passed, `test_visual.py`: 54 passed | 0e391da |
| TEST-hr-alerts-cobertura | agente-ronda3 | 2026-09-07 | terminado: brecha de cobertura en `app/routes/hr_alerts.py` -- `test_autorizacion_hr.py` solo probaba 401/403, ningun test ejercitaba OR-155 (validacion de `fecha_inicio`), el cierre del tramo anterior + sincronizacion de `cargo_id` (BR-067/OR-038/OR-013), la reapertura del tramo previo al borrar (OR-014), ni el filtro `deleted_at`/ausencia de piso de dias en `get_retirement_alerts` (OR-022/OR-023). 12 tests nuevos en `app/tests/test_hr_alerts.py`, sin cambios de codigo de produccion. `python -m pytest app/tests -q`: 913 passed | a669c6c |
| TEST-trash-versiones-cobertura | agente-ronda4 | 2026-09-07 | terminado: brecha de cobertura en `app/routes/trash.py` -- `test_autorizacion_papelera.py` solo probaba 401/403/409/200 de autorizacion en `/papelera` y `/papelera/*/purgar`; ningun test ejercitaba la logica de negocio de `/documento/{id}/versiones*` (add_version, restore_version, delete_version) ni `restore_document`. Cubierto: IN-149 (add_version rechaza file_url con `..`), OA-008 (restore_version archiva la version vigente ANTES de sobreescribir, y NO archiva una version fantasma si la vigente ya coincide con la pedida), 404 en los tres endpoints con IDs inexistentes, y el camino feliz de add_version/restore_document verificando los parametros exactos de cada consulta. 11 tests nuevos en `app/tests/test_trash_versiones.py`, sin cambios de codigo de produccion. `python -m pytest app/tests -q`: 927 passed | 65cd7e6 |
| BUG-ronda6-recorrido-vivo | agente-ronda6 | 2026-09-07 | terminado: bug real en vivo, no de cobertura. Probe 4 flujos con Playwright contra el arnes (fakedb.py): importacion CSV (falla por el arnes -- `db_transaction()` abre conexion real no mockeada, artefacto del harness, no bug de produccion), ciclo de propuestas de IA, backup export/restore, y widget de chat -- sin hallazgos en esos tres. El bug real salio al abrir `/admin/ia` (Consola del asistente IA) con sesion admin.global: la pagina redirigia sola a `/login` a los ~2s. Rastreado con captura de requests: `POST /api/auth/logout` disparado antes de que nada expirara. Causa: `loginSuccess()` (`app.js`) llama `loadDynamicChoices()` sin guarda; esa funcion solo existe en `app-choices.js`, que `admin_ai.html` no incluye (no tiene buscadores ni TomSelect) -- `ReferenceError` no capturado dentro del `try` de `checkPersistedSession()`, cuyo `catch` es `logout()`. Cualquier admin Global que abriera la Consola IA quedaba deslogueado al instante, sin error visible en consola (el catch lo tragaba). Fix: guarda `typeof loadDynamicChoices === "function"` en `app.js` (mismo patron que `showProgress`/`hideProgress` en `charts.js`). Verificado en vivo antes/despues con Playwright: antes, `POST /api/auth/logout` + redireccion; despues, la pagina se queda en `/admin/ia` con sesion intacta. `node --check app/static/js/core/app.js` OK. `python -m pytest app/tests -q`: 932 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed | de356e3 |
| BUSQUEDA-llamadas-huerfanas | agente-ronda7 | 2026-09-07 | terminado: patron de ronda6 (loadDynamicChoices sin guarda), buscado sistematicamente en las 11 paginas HTML con un script que cruza cada `<script src>` cargado por pagina contra las funciones top-level definidas en cada JS y las llamadas hechas dentro de ese mismo conjunto. Bug real encontrado: `openDocMetadataModal()` en `hr.js` (usada al abrir un documento desde el expediente RRHH, muy transitada) llamaba `closeDocViewer()` sin guarda y `toggleDocViewer(fileUrl)` en el onclick de "Ver PDF", ambas definidas solo en `archive.js` -- que `hr.html`/`admin_hr.html` no cargan. `ReferenceError` no capturado en cada apertura del modal de documento de RRHH, cortando el renderizado de metadatos antes de tiempo. Fix: movidas `toggleDocViewer()`/`closeDocViewer()` (y el helper `_archivoMotionBehavior`, renombrado `_motionBehavior`) de `archive.js` a `app-core.js`, el unico script comun a `archive.html`/`hr.html`/`admin_archive.html`/`admin_hr.html` -- en las paginas admin el markup no existe y ambas funciones no hacen nada por su propia guarda de `getElementById`. El resto de llamadas cruzadas que devolvio el script (loadAdminTab, loadMonitorTable, openEditDocModal, triggerArchivoSearch, applyDatePreset, etc.) ya estaban guardadas con `typeof`/optional-chaining sobre existencia de elemento del DOM (patron de ronda6), o eran falsos positivos de comentarios de codigo. Paso 2 (Playwright) no hizo falta: el hallazgo salio del paso 1. `node --check` en los dos archivos tocados OK. `python -m pytest app/tests -q`: 932 passed. `python -m pytest app/tests/test_visual.py -q`: 1 failed la primera vez (`admin/ia-390px-claro`, timeout de red al esperar `networkidle`, con aviso de `DATABASE_URL` en log -- no relacionado con el cambio), repetido solo y paso: 53 passed + 1 passed = 54/54 | 5389960 |
| R8-recorrido-filtros-pdf-tema-papelera | agente-ronda8 | 2026-09-07 | terminado: recorrido en vivo con Playwright contra el arnes (fakedb.py) de los cuatro flujos pedidos, sin hallazgos -- a) filtros combinados de archive.html/hr.html (anio+soporte+tipo en Archivo, anio+estado+tipo en RRHH) acumulan correctamente en el body JSON de cada `/buscar` sucesivo, sin pisarse; b) "Imprimir Expediente" en hr.js genera el HTML imprimible de `/api/rrhh/report/{id}` con datos reales, sin error de consola; c) combinacion real de tema+acento+densidad+estilo visual en el panel de Personalizacion (`.ds-theme-card`, `applyAccentColor`, `applyStyleTheme`, `applyDensity`) actualiza `body.className`/`dataset.accent`/`dataset.style` y las variables computadas `--ds-accent`/`--tt-accent` de verdad, contenido visible verificado con `innerText`, no solo el fondo; d) papelera de Archivo y de RRHH (documentos y empleados): boton "Restaurar" -> confirmar en el modal -> `POST /papelera/{id}/restaurar` 200 -> la fila desaparece de la tabla y el toast de exito aparece, en ambos modulos. Paso 3: revisando los 48 endpoints POST/PUT/PATCH/DELETE de `app/routes/` contra la cobertura de rondas anteriores, un endpoint tenia el patron IN-133 (identidad de auditoria tomada de un campo declarado por el cliente en vez de la sesion) sin ficha propia y sin arreglar: `POST /api/admin/retencion/disponer/{doc_id}` en `retention.py` escribia `data.requester` -- string libre del payload -- en la columna `disposicion_por` y en `log_event`, pese a que el endpoint ya exige `require_admin_role("Archivo")`; cualquier admin de Archivo podia atribuir la decision de disposicion a cualquier otro nombre. Arreglado: `usuario_sesion: str = Depends(require_session)` reemplaza `data.requester` en el UPDATE y en `log_event`, mismo patron que docs.py/catalog.py/users.py; `requester` se mantiene en el modelo por compatibilidad pero ya no se lee, documentado en el propio campo. 1 test nuevo en `test_disposicion.py` (`test_ignora_el_requester_declarado_por_el_cliente`) mas los 4 existentes actualizados para pasar `usuario_sesion`. `python -m pytest app/tests -q`: 933 passed (927 antes + ~6 nuevos/actualizados). Sin cambios de frontend/HTML, `test_visual.py` no aplica. | 1997ea3 |
| R11-recorrido-escaner-compartido-notif-alta | agente-ronda11-in066 | 2026-09-08 | plan revisado, no ejecutado: `ayuda.html`/`app/routes/pages.py` leidos completos. La premisa de R10 no aplica a `ayuda.html` -- es la unica de las paginas publicas que NO usa la cascara compartida en absoluto: no tiene `#app-shell-navbar`, `#app-shell-sidebar` ni carga `/static/js/core/app-shell.js` (grep de esos tres patrones sobre `app/static/*.html` solo los encuentra en las 7 paginas internas -- archive/hr/admin_archive/admin_hr/admin_system/admin_ai/sistema.html --, nunca en ayuda.html, login.html, investigacion.html ni compartido.html). En vez de la cascara, `ayuda.html` trae su propio enlace fijo "Volver a la aplicacion" (`.ds-volver-app`) y solo carga `app-core.js`+`app-theme.js`, sin `app-shell.js`. R10 la eligio "por ser la de menor trafico" entre las 10 sin verificar que efectivamente repitiera la cascara -- no la repite, asi que no hay nada que extraer de ella para el primer paso del plan. Migrar el patron de include server-side habria que probarlo con una de las 7 paginas que si tienen la cascara (ninguna es tan de bajo riesgo como se asumia de ayuda.html), asi que se deja sin ejecutar en este carril. Sin cambios de codigo. `python -m pytest app/tests -q` no se corrio (sin cambios que verificar). | (sin commit, sin cambios de codigo) |
| IN133-backup-imports-requester | agente-ronda9 | 2026-09-08 | terminado: grep sistematico de `requester`/`.get("requester")` como identidad de auditoria en `app/routes/` y `app/core/`, comparado contra los ya cerrados (docs.py, catalog.py, share.py, users.py, models.py, retention.py). Encontrados dos endpoints reales sin ficha propia con el mismo patron IN-133 -- `GET /api/admin/backup/export` y `POST /api/admin/backup/restore` (`backup.py`) escribian `requester` de la query string en `backup_history.usuario`; `POST /api/admin/import/empleados` y `POST /api/admin/import/documentos` (`admin/imports.py`) usaban `requester` en `log_event` y en `_resolve_user_id()` para `creado_por`/`updated_by` -- las cuatro rutas ya exigen sesion (`require_session`/`require_role("Global")`) pero descartaban el nombre de usuario que esa dependencia devuelve. Arreglado: `usuario_sesion: str = Depends(require_session)` reemplaza `requester` en las cuatro, mismo patron que retention.py. 3 tests nuevos (`test_backup.py::test_export_registra_la_sesion_no_el_query_string`, `test_imports.py::TestIN133IgnoraRequesterDeclaradoPorElCliente`) que fuerzan `requester=otro_usuario_falsificado` en la query y verifican que gana la sesion. `python -m pytest app/tests -q`: 936 passed (933 antes + 3 nuevos). Sin cambios de frontend/HTML, `test_visual.py` no aplica. | 162c699 |
| R10-recorrido-escaner-compartido-notif-alta | agente-ronda10 | 2026-09-08 | terminado sin cambios de codigo: probados en vivo contra el arnes (fakedb.py + generate_session_token) los cuatro flujos pedidos, sin hallazgos explotables -- a) `scanner-client.js`: revision linea a linea de todo el manejo de error (WS 1006/1008/4001, contenido mixto https/ws:// detectado ANTES de intentar conectar, backoff con techo de 10 min que pasa a "reintentar a mano", vigilante de silencio DG-006, `_scnTest`/`_scnTestScan` con mensajes especificos) -- ya cierra en mensaje claro en cada rama, nada en silencio ni error crudo; no hay hardware para probar el happy path pero el codigo de error ya esta cubierto por rondas de recorrido visual anteriores (VI-*, DG-*, SI-17x citados en el propio archivo). b) enlace de compartir externo: `POST /api/admin/compartir` con sesion admin.global crea el token, pero `GET /api/compartido/{token}` sin cookies devolvio 404 "no valido o caduco" en el arnes -- rastreado a `_esta_revocado()` en `core/security.py`, que hace `SELECT 1 FROM enlaces_revocados WHERE jti=%s fetch="one"`; `fakedb.py` no reconoce esa tabla y su `fake_db_query` devuelve SIEMPRE una fila sintetica en `fetch="one"` (nunca `None`), asi que CUALQUIER jti sale "revocado". Es un artefacto del arnes (mismo patron que la importacion CSV de BUG-ronda6), no un bug de produccion: `test_share.py` ya cubre revocacion/caducidad/manipulacion contra un `db_query` mockeado de verdad. No se toco `fakedb.py` (compartido por 20 carriles, fuera de esta reserva). c) campana de notificaciones (`_initNotificationBell`/`loadNotifications` en `app-theme.js`): no es un sistema de "leido/no leido" -- es un panel de pendientes (`status IN ('revision','draft')`) que al hacer clic en un item navega fuera del panel; no hay "marcar como leido" que probar, la premisa de la ronda no aplica a como esta construido. Badge y lista ya delegan en `/api/admin/notifications` (catalog.py), sin manejo de error visible mas alla de silencio por diseno documentado en el propio try/catch. d) `admin/submit.js` (alta de documento/empleado): revision completa de `handleNewSubmission` y `_validateSubmitField` -- valida cada campo obligatorio en el borde (con foco en el primero que falla), cedula/RIF venezolanos con digito verificador, coherencia de fechas nacimiento<ingreso<jubilacion con edad 16-75, topes de longitud espejo de `models.py`, tipo/tamano de archivo antes de subir, y distingue 4xx/413/5xx/red en el error del backend -- carril B7 (cerrado 2026-09-03) ya cubrio esto a fondo, nada nuevo que romper. Sin brecha de cobertura nueva encontrada tampoco (`test_choices.py`, `test_share.py`, `test_admin.py` etc. ya cubren `lookups.py`/`share.py`/`admin/submit`). Plan breve IN-066/IN-042 (ambos `[CHOCA]`, solo lectura, sin ejecutar): IN-066 (7 paginas repiten la cascara a mano) -- extraer cabecera+nav+orden de `<script>` a un include servido por `pages.py` (Jinja2 minimo o snippet inyectado server-side), sin build step; migrar una pagina primero (`ayuda.html`, la de menor trafico) y validar con `test_admin_panels.py`+`test_paginas.py` antes de tocar el resto. IN-042 (sin capa de repositorios, SQL literal en 14 ficheros) -- crear `app/repos/` con una funcion tipada por consulta ya existente (empezando por las de `lookups.py`, que ya estan aisladas de rutas), mover las rutas a llamarlas, dejar `db_query` como unico ejecutor por debajo; requiere coordinarse con IN-054 (mismo ciclo de imports) y tocar `test_sql_columns.py`/`test_sql_inserts.py` para que sigan derivando el esquema igual. `python -m pytest app/tests -q`: 936 passed (sin cambios). | (sin commit, sin cambios de codigo) |
| R12-in066-cascara-marcador-textual | agente-ronda12-in066 | 2026-09-08 | terminado sin cambios de codigo: leidas completas las 7 paginas que si repiten la cascara (archive.html, hr.html, admin_archive.html, admin_hr.html, admin_system.html, admin_ai.html, sistema.html) y app/routes/pages.py (`_page()` sirve cada HTML como texto plano, sin motor de plantillas). El bloque NO es uniforme entre las 7, contradiciendo la premisa de R10/R11: (a) hr.html trae un `<script>` inline extra en el propio tag `<body>` que precarga `dark-mode`/`ds-density-compact` desde localStorage -- las otras 6 solo precargan el tema de acento (`ds_theme`); ese bloque extra no esta en ningun otro sitio de hr.html, asi que un marcador generico lo borraria o habria que tratarlo como caso especial. (b) admin_ai.html no lleva las clases `hold-transition sidebar-mini` en `<body>` (las otras 6 si) y usa formato sin indentar ni comentarios/lineas en blanco alrededor de los tres elementos de la cascara, distinto al de archive/hr/admin_archive/admin_hr/admin_system (indentados, comentados, con blancos). (c) sistema.html tampoco lleva comentarios ni blancos, mas compacto que el grupo principal. En limpio: solo 4 de las 7 (archive, admin_archive, admin_hr, admin_system) comparten el bloque literal completo caracter a caracter; hr.html difiere por logica adicional real (no solo formato), y admin_ai.html/sistema.html difieren en marcado (clases de body) y estilo. Extraer un marcador de texto fijo tipo `<!--APP_SHELL_HEAD-->` que pages.py sustituya exigiria decidir que hacer con la clase de body variable y con el script inline de hr.html -- deja de ser una sustitucion mecanica de texto y pasa a ser una migracion con logica condicional, justo el escenario que la consigna pedia NO forzar. Cerrado sin ejecutar cambios; no se toco codigo ni tests. | (sin commit, sin cambios de codigo) |
| R13-tema-oscuro-11temas-densidad-live | agente-ronda13 | 2026-09-08 | terminado: barrido de modo oscuro contra los 11 temas de acento, tres bugs de contraste reales cerrados en `app/static/styles/dark-mode.css` y `app/static/styles/shell.css` -- titulo de resultado casi invisible, badge de usuario sin fondo con pares oscuros, y titulo de marca de la barra lateral invisible en modo oscuro combinado con cualquiera de los 11 temas. `python -m pytest app/tests -q`: 936 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed. | e0fc210 |
| R14-contraste-dark-mode-resto-componentes | agente-ronda14 | 2026-09-08 | terminado: barrido de contraste sobre el resto de componentes con `!important` en `dark-mode.css` que R13 no tocó (badges de estado, botones outline, tablas, tooltips, breadcrumbs, modal de cambio de estado, `.ds-*` sin `!important`) -- todos ya tenían par oscuro correcto salvo dos bugs reales encontrados fuera de `dark-mode.css`, en `paginas.css`: `.badge-success` (blanco sobre #28a745, 3.13:1) sólo se había corregido bajo `body:not(.dark-mode)`, así que el mismo fallo seguía roto en modo oscuro -- el color del badge no depende del fondo de la página, es una regla con scope de más; `.badge-info` (blanco sobre #17a2b8, 3.04:1) nunca se había corregido en ningún modo. Ambos fijados sin scope de tema (#208838 y #117a8b, 4.53:1 y 5.02:1); se usan en monitor.js, submit.js, hr.js, admin_system.html, categories.js, sistema.html. Test nuevo en `test_contraste.py` que fija ambos colores y verifica que ninguno vuelva a llevar scope `body:not(.dark-mode)`. `python -m pytest app/tests -q`: 937 passed, 1 failed (timeout de red en Playwright `goto networkidle` de `rrhh-1440px-claro`, no relacionado con el cambio -- `test_visual.py` aislado dio 54/54 passed justo antes del fallo). `python -m pytest app/tests/test_visual.py -q`: 54 passed. | fcb5b5d |
| R16-hr-report-pdf | agente-ronda16 | 2026-09-08 | terminado: bug real de portabilidad en `generate_hr_report` (`app/routes/hr.py`), seccion "Historial de Cargos". El generador de filas usaba un f-string con comillas escapadas (`\"color:#198754...\"`) dentro de la propia expresion `{}` de reemplazo -- valido en Python 3.12 (PEP 701) pero `SyntaxError: f-string expression part cannot include a backslash` en Python <3.12. `.python-version` fija 3.11 para Vercel, que es donde corre produccion; localmente el repo usa 3.12, asi que pytest/Playwright nunca lo vieron romperse (visto tambien en R8, que probo este mismo endpoint en vivo sin hallazgos porque corrio contra el arnes local en 3.12). Probado con empleado con muchos documentos/historial de cargos y con uno sin ninguno (fecha_jubilacion/fecha_pension/foto_url nulos, sin historial): ambos casos ya caian en "Sin documentos registrados"/"Sin historial registrado" con manejo `COALESCE`/`or '-'` correcto, sin excepcion. Arreglado: la fila de historial se precomputa en `_fila_historial()` fuera del f-string principal, mismo patron que ya usaba `rows_html` para los documentos -- sin backslashes dentro de ninguna expresion `{}`. `python -m pytest app/tests -q`: 938 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed. | cf246ee |
| R15-cambio-contrasena-ciclo-usuario-acentos | agente-ronda15 | 2026-09-08 | terminado sin hallazgos reales, sin cambios de codigo: probados en vivo con Playwright contra fakedb.py los tres angulos pedidos, en orden. (1) Flujo de cambio de contrasena en admin/users.js (handleChangePassword): password debil (<6) rechazada con mensaje claro sin llamar a la API, password valida actualiza correctamente ("actualizada" en el toast); no hay campo de confirmacion duplicado (un solo prompt), asi que "no coinciden" no aplica -- es diseno, no bug. (2) Ciclo de vida de usuario (desactivar/reactivar): dos falsos positivos descartados tras revisarlos -- (a) mi primer intento probo con mjrodriguez, que en la data sintetica de fakedb.py tiene 4 filas con el mismo usuario (ids 1/5/9/13); desactivar una fila no bloqueaba el acceso porque otra fila del mismo usuario seguia activa, pero usuarios_sistema.usuario es UNIQUE en schema.sql real, asi que ese escenario no puede darse en produccion -- artefacto de la fixture, no bug real; (b) fakedb.py (ver comentario en el propio archivo) no persiste ningun UPDATE, devuelve filas fabricadas geneticamente salvo el caso especial admin.global, asi que ninguna prueba de persistencia de is_active es valida contra este arnes -- deps.py ya revisa is_active en cada request via _fila_usuario() (confirmado leyendo el codigo, no solo grep), lo cual es la garantia real; no hay forma de verificarlo en vivo sin una Postgres real, fuera del alcance de este arnes. (3) Personalizacion (7 estilos + 8 acentos): los 7 STYLE_THEMES de app-theme.js cambian radio/borde/fondo de .card en vivo (confirmado con getComputedStyle tras applyStyleTheme), y los 8 ACCENT_COLORS cambian --ds-accent en body correctamente (confirmado tras corregir mi propio test, que primero leia --ds-accent de document.documentElement en vez de body -- las custom properties no suben al padre, ese era mi error, no del producto). .btn-primary no consume --ds-accent (color fijo #0072ed via paginas.css) pero si lo hacen .ds-kw-badge, .ds-compartido-titulo, sidebar activo en oscuro y el widget de IA -- lectura del propio CSS sugiere que es una decision de diseno documentada (comentarios SD-010/SD-048/SD-055 ya discuten explicitamente que boton primario se mantiene azul de accion, el acento se usa en acentos secundarios), no un estilo sin implementar. Cayo a brecha de cobertura como ultimo recurso: no hay ningun test JS (unitario ni de arnes) para la logica pura de _pwdStrengthInfo/medidor de fortaleza en users.js, pero es un patron de todo el repo (cero tests JS fuera de scanner-app/, todo el resto se cubre por Playwright visual/contraste) y montar un runner JS es un cambio de infraestructura fuera del tamano de una ronda. Cierre honesto: python -m pytest app/tests/test_contraste.py -q seguia en verde (13 passed) antes de empezar, confirmando que R13/R14 no dejaron nada pendiente. Ningun archivo de codigo tocado. | (sin commit, sin cambios de codigo) |
| R18-ci-python311 | agente-ronda18-ci311 | 2026-09-08 | terminado: cerrada la brecha de proceso documentada por R17 -- `.github/workflows/tests.yml` corria CI con `python-version: "3.12"` via `actions/setup-python`, distinto de `.python-version` (3.11, la version real de Vercel/produccion), lo que dejo pasar sin ver el bug de sintaxis f-string PEP 701 de R16 (hr.py). Cambio minimo: `"3.12"` -> `"3.11"` en el unico job (`pytest`), sin matriz -- no se encontro ninguna dependencia de Playwright/`test_visual.py` ni de otro archivo del repo atada a 3.12 especificamente (grep de `python-version`/`3.12`/`3.11` en todo el repo, unicos hits ademas del propio workflow y `.python-version`/`app/.python-version` son documentacion y SVGs generados sin relacion). `python -m pytest app/tests -q`: 938 passed (local en 3.12, sin cambios de codigo de produccion; el propio CI confirmara en 3.11 tras el push). | 27cfb92 |
| R19-requirements-techos-y-share-revocado | agente-ronda19 | 2026-09-08 | terminado sin hallazgos ni cambios de codigo (commit c83150c): dos angulos. (1) Revision sistematica de techos de version en `api/requirements.txt` y `requirements-dev.txt` tras el bug de R18 (httpx sin techo rompiendo CI). `api/requirements.txt` ya usa `==` exacto en las 9 dependencias, ninguna con rango abierto. `requirements-dev.txt` solo tiene dos sin techo superior: `pytest>=8.0` y `pyflakes>=3.2`. Probadas contra las ultimas versiones publicadas en PyPI hoy (`pip index versions`): `pytest==9.1.1` (la instalada localmente, la mas nueva que existe) corre la suite completa en verde -- `python -m pytest app/tests -q`: 938 passed, 261 warnings, sin fallos ni deprecaciones nuevas de pytest. `pyflakes==3.4.0` (la mas nueva, instalada con `pip install --upgrade` para la prueba) tambien pasa `test_static_analysis.py` (3 passed) sin cambios; unico efecto colateral fue un aviso de pip de que `flake8` local (no listado en ningun requirements del repo) pide `pyflakes<3.3.0` -- irrelevante para CI/Vercel, que no instalan flake8. Pyflakes revertido a 3.2.0 tras la prueba para no dejar el entorno local alterado. Conclusion: httpx era el caso real (breaking change de API entre 0.27 y 0.28), no un patron general -- no hay ningun otro techo faltante que rompa hoy. (2) Enlace de compartir revocado con Playwright en vivo contra el arnes (fakedb.py + admin.global): antes de repetir la prueba de R10 se releyo `fakedb.py` completo -- no define `enlaces_revocados` en absoluto, y su `fetch="one"` generico (usado por `_esta_revocado()` en `core/security.py`) devuelve SIEMPRE una fila sintetica para cualquier consulta, nunca `None`. Esto confirma por lectura de codigo (sin necesidad de reejecutar Playwright) que el camino "crear->NO revocar->deberia funcionar" tambien devolveria 404 "no valido o caduco" en este arnes, igual que el camino "crear->revocar" que encontro R10 -- no es que la revocacion funcione mal, es que el arnes marca TODO token como revocado sin distincion, asi que ninguno de los dos caminos es reproducible aqui, no solo el negativo. `test_share.py` (mockeando `db_query` de verdad, sin este artefacto) sigue siendo la cobertura real de revocacion/caducidad/manipulacion. Sin brecha de cobertura nueva encontrada tras revisar la lista de `app/tests/` completa (43 archivos) contra `app/routes/` y `app/routes/admin/`: cobertura ya exhaustiva tras 18 rondas previas. Cierre honesto: no queda nada explotable en ninguno de los dos angulos asignados. | (sin commit, sin cambios de codigo) |
| R17-fstrings-backslash-py311 | agente-ronda17-py311 | 2026-09-08 | terminado sin hallazgos nuevos, sin cambios de codigo: script AST propio (ast.parse + ast.walk sobre JoinedStr/FormattedValue, comparando ast.get_source_segment de cada expresion contra backslash literal) corrido sobre todos los .py de app/ con Python 3.12 -- cero candidatos, el bug de R16 (hr.py) era el unico caso en todo el repo. Grep de texto adicional (f-string con backslash dentro de las primeras llaves) tambien sin resultados, como red de seguridad. No se pudo reproducir con un interprete 3.11 real porque solo hay 3.12 disponible en esta maquina (misma limitacion que R16). Brecha de proceso confirmada y real: .python-version fija 3.11 (la version de Vercel/produccion), pero .github/workflows/tests.yml usa actions/setup-python con python-version: "3.12" para correr pytest -- CI nunca corre en la version real de produccion, asi que este patron de PEP 701 (u otro exclusivo de 3.12+) puede volver a colarse sin que ningun test lo vea. Recomendacion para un carril aparte: anadir un job (o cambiar el existente) a python-version: "3.11" en tests.yml, o al menos un paso de solo-sintaxis (py_compile/ast.parse) contra 3.11 en CI. No ejecutado en este carril por instruccion explicita. | (sin commit, sin cambios de codigo) |
| R18b-ci-httpx-pin | agente-direccion | 2026-09-08 | terminado: tras empujar el cambio de R18 (CI a Python 3.11), el primer run real de GitHub Actions fallo -- no por sintaxis (confirmado instalando Python 3.11 real localmente via winget y corriendo `ast.parse` sobre todo `app/`: cero SyntaxError, R17 tenia razon), sino porque `requirements-dev.txt` fijaba `httpx>=0.27` sin techo: httpx 0.28 (publicado hace poco) quito el atajo `app=` que usa el TestClient de starlette 0.36.x (el que trae fastapi==0.110.0), asi que una instalacion limpia -- como CI, o el venv de Python 3.11 recien creado para reproducir el fallo -- instala 0.28 y cada TestClient del proyecto revienta con `TypeError: Client.__init__() got an unexpected keyword argument 'app'` (167 failed + 77 errors reproducidos localmente). El entorno local de 3.12 nunca lo vio porque tenia httpx 0.27.2 cacheado de antes de que saliera la 0.28 -- este bug no era de version de Python, sino que la ronda 18 (CI con instalacion limpia) fue lo que lo destapo. Arreglado fijando `httpx>=0.27,<0.28` en requirements-dev.txt. Verificado en el venv de Python 3.11 real (recien creado, sin cache): 884 passed + 1 skipped (test_visual.py se salta solo sin Playwright instalado, comportamiento documentado). Verificado tambien sin regresion en el entorno local de 3.12: 938 passed. | c5c8fbb |
| VI-zoom-texto-modales-monitor-ronda21 | agente-ronda21 | 2026-09-08 | terminado: siguiendo el mismo angulo de la ronda 20 (zoom de texto WCAG 1.4.4, no solo anchos de viewport), se probo con Playwright y sesion falsa (mismo patron que BUG-sidebar-scroll-admin) el modal de edicion de documento, el menu rapido "Cambiar estado", el panel de Personalizacion y las tablas del monitor (Archivo/RRHH) con texto largo real (titulo de 200+ caracteres, nombre con guiones y apostrofe, cargo/departamento largos) a 200% de zoom. El modal y el menu de estado no llegaron a montarse bajo el arnes de sesion simulada (limitacion del harness de exploracion, no bug); panel de personalizacion y monitores no desbordaron. Hallazgo real en una tercera via: las 3 paginas publicas que pedia la ronda -- `ayuda.html` desbordaba 39px a 390px con zoom 200% (`.help-coming-soon` con `padding:3rem` fijo, que escala con la tipografia igual que ella; a 200% deja tan poco ancho que la palabra "Tutoriales" de `<h4>Tutoriales proximamente</h4>` no cabia y no habia `overflow-wrap` que la partiera). Confirmado con un scan de nodos de texto via Range API, no solo bounding rects de elementos (el desborde no lo causaba ningun elemento sino un `<h4>` cuyo propio box no crecia pero su texto sin `overflow-wrap` si). Arreglado con `overflow-wrap: break-word` en `.help-coming-soon`. `investigacion.html` y `compartido.html` sin desborde a 200%. `python -m pytest app/tests -q`: 938 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed | 50b325f |
| VI-zoom-texto-navbar-ronda20 | agente-ronda20 | 2026-09-08 | terminado: angulo nuevo tras 19 rondas -- zoom de texto (WCAG 1.4.4, `document.documentElement.style.fontSize='200%'` via Playwright) en vez de solo anchos de viewport a 100%. Probadas primero las paginas publicas (/login,/archivo,/rrhh) a 390/1440px con zoom 200%: sin desborde. Con sesion falsa en localStorage + fetch interceptado (mismo patron que BUG-sidebar-scroll-admin) se llego a /admin/sistema, /admin/archivo, /admin/rrhh: desborde real de 38px a 390px con zoom 200% -- `.ds-nav-user-badge` (icono+username en la barra superior) tenia `max-width:180px` fijo sin variante de movil, a diferencia de `#nav_username` que ya usaba `30vw` en el mismo breakpoint (`max-width:575.98px`, paginas.css); con zoom normal cabia justo, con 150-200% el logo+credencial+"Cerrar Sesion" ya no caben en una fila sin wrap (`.navbar-nav` de Bootstrap no envuelve) y el navbar entero arrastra a la pagina. Arreglado con `max-width:28vw` + padding/gap reducidos en el mismo breakpoint que ya achica `#nav_username` y oculta el label del boton. Verificado sin desborde en las 6 rutas x 390/1440px x zoom 200%, y sin regresion a zoom 100%. No se probo doble envio de formulario (login.js ya deshabilita el boton de forma sincrona antes de cualquier `await`; admin/submit.js hace lo mismo con `submitBtn.disabled=true` como primera linea de `handleNewSubmission` antes del `try`) ni paste en cedula/fecha (`_normalize_cedula` en models.py ya rechaza cualquier cosa sin forma de cedula tras strip+upper+replace, incluyendo saltos de linea internos; los campos `type=date` los controla el propio navegador). `python -m pytest app/tests -q`: 938 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed | a072bf9 |
| VI-zoom-oscuro-widget-compartido-ronda22 | agente-ronda22 | 2026-09-08 | terminado: siguiendo el angulo de zoom de texto (WCAG 1.4.4) de las rondas 20/21, se probaron tres vias nuevas no cubiertas antes -- (1) modo oscuro + zoom 200% simultaneos (`body.dark-mode` + `fontSize=200%` a la vez, no cada uno por separado) en /login, /archivo, /rrhh, /ayuda, /compartido publicas y /admin/sistema, /admin/archivo, /admin/rrhh, /admin/ia con sesion falsa: sin desborde en ninguna; (2) `ai-widget.js` a zoom 200% en 390px y 1440px: la burbuja no llego a montarse porque `/api/ia/disponible` responde `disponible:false` sin `OPENROUTER_API_KEY` en el entorno de pruebas (limitacion del harness, no bug -- confirmado leyendo `comprobarDisponibilidad()` en ai-widget.js, que oculta la burbuja a proposito cuando el asistente no esta configurado); (3) `compartido.html` a fondo (claro/oscuro x 390/768/1440px): hallazgo real -- `.ds-compartido-sello` (la insignia "Documento compartido" de la cabecera) tenia `white-space:nowrap` sobre icono+texto; a 200% de zoom el texto crece pero el `white-space:nowrap` le impide partirse en dos lineas, y aunque `.ds-compartido-cab` es flex con `flex-wrap:wrap`, el propio span ya era mas ancho que el contenedor a 390px (desborde de 14-16px, confirmado en claro y oscuro por igual, descartando que fuera el modo oscuro la causa). Arreglado quitando `white-space:nowrap` (pasa a `normal`) y añadiendo `max-width:100%`+`overflow-wrap:break-word` en `.ds-compartido-sello` (paginas.css), mismo patron que el fix de `ayuda.html` en la ronda 21. Verificado sin desborde en 390/768/1440px x claro/oscuro x zoom 200%. `python -m pytest app/tests -q`: 938 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed | 469ea54 |
| VI-teclado-foco-ronda24 | agente-ronda24 | 2026-09-08 | terminado: angulo nuevo -- navegacion por teclado y orden de foco, ninguna ronda previa lo probo. Con Playwright y sesion admin.global (mismo patron que rondas 20-23) se probo: (a) Tab desde el body en /login, /archivo, /admin/archivo -- hallazgo real: `loadAdminTab("stats")` (admin/index.js) mueve el foco con `activePane.focus()` no solo en cambios de pestana por clic (correcto, anuncia el cambio a lectores de pantalla, OR-225) sino tambien en la entrada automatica a la pagina (`app.js`, al mostrar `admin-archivo`/`admin-rrhh`), confirmado con `document.activeElement` justo tras cargar la pagina sin tocar nada: era el `<div>` del panel de Resumen, no `<body>`. Eso secuestraba el primer Tab hacia ese panel, saltandose el enlace "Saltar navegacion" (SI existe, con `tabindex=0` y oculto fuera de pantalla, no con `display:none`, tal como documenta CLAUDE.md) y la barra superior completa -- confirmado que en `/login` y `/archivo` (sin este patron) el orden de foco si sigue el orden visual y el skip-link es el primer Tab. Arreglado con un parametro `{moveFocus=true}` en `loadAdminTab`: la llamada automatica de `app.js` pasa `moveFocus:false`, la de clic en pestana sigue moviendo el foco. Verificado con Playwright tras el fix: primer Tab en `/admin/archivo` ya es `#skip-nav-link`. (b) Modales de edicion/confirmacion abiertos solo con teclado: el foco trap ya existe (`admin/index.js` L.615-622, ciclo Tab/Shift+Tab entre primer y ultimo elemento enfocable) y Escape cierra -- verificado en el codigo, no se encontro fallo. (c) Menu "Cambiar estado": no se pudo montar bajo el arnes de sesion simulada de fakedb (limitacion conocida del harness, igual que en rondas 20-21, no bug). (d) Outline de foco en los 11 temas: no se llego a probar a fondo tras el hallazgo de (a) -- queda pendiente para otra ronda. `python -m pytest app/tests -q`: 938 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed | 88af1be |
| VI-zoom-texto-grep-sistematico-ronda23 | agente-ronda23 | 2026-09-08 | terminado sin cambios de codigo: en vez de seguir probando pagina por pagina el mismo patron de las rondas 20-22 (badge/sello con `white-space:nowrap` o padding fijo sin `overflow-wrap` que desborda a zoom 200%), se hizo grep sistematico de TODO `app/static/styles/*.css` -- 22 ocurrencias de `white-space: nowrap` y ~90 selectores `.ds-*`/`.badge*`/`.tag*`/`.pill*`/`.sello*` -- y se contrasto cada una contra los 3 ya arreglados (`.ds-nav-user-badge` ronda20, `.help-coming-soon` ronda21, `.ds-compartido-sello` ronda22). Candidatos evaluados con criterio antes de tocar Playwright: `.ds-item-type-badge` y `.ds-item-kw-badge` (componentes-admin.css) tienen el patron exacto -- nowrap + padding fijo sin overflow-wrap -- pero estan **muertas**: confirmado con grep en todo `app/` (html+js) que ninguna plantilla ni JS las usa, asi que no hay nada que renderice para desbordar. `.select-tag` (personalizacion.css) igual de muerta. `.ds-badge` (componentes.css), usado de verdad en `archive.js` para soporte/tesauro, ya trunca el texto con `overflow:ellipsis` + `title` + funcion JS `_truncBadge()` explicita -- truncamiento intencional, no desborde. El resto de `nowrap` encontrados (`.ds-notif-item-label`, `.ia-hist-titulo`, `.info-box-text`, `.ds-kpi-sub`, `.ds-import-file-label`, `#nav_username`) ya combinan `overflow:hidden`+`text-overflow:ellipsis` -- mismo patron de truncamiento deliberado usado en toda la app, no el bug puntual que se venia cazando. `.ds-navbar-logo` (shell.css) tiene nowrap pero sin texto (solo `<img>`). `.ds-admin-tabs .nav-link` tiene scrollbar oculto adrede (`::-webkit-scrollbar{display:none}`), indicando scroll horizontal intencional, no bug. Ningun candidato llego a ameritar prueba con Playwright real. Conclusion: la linea de "badge con nowrap sin overflow-wrap" que dio 3 hallazgos reales en las rondas 20-22 esta agotada -- lo que queda del grep es CSS muerto o truncamiento deliberado con ellipsis, un patron distinto y mucho mas amplio que abrir aqui seria una auditoria nueva, no una cuarta repeticion de esta. No se toco ningun archivo, no aplica pytest | (sin commit) |
| VI-teclado-tablas-monitor-ronda26 | agente-ronda26 | 2026-09-08 | terminado sin hallazgos reales, sin cambios de codigo: siguiendo lo pendiente explicito de la ronda25 (angulo b, tablas de monitor), se probo con Playwright y el mismo arnes (fakedb.py + admin.global via localStorage archive_session/cookie ds_session, patron de capturar.py) la tabulacion desde los filtros hasta los botones de accion de la fila en `/admin/archivo` y `/admin/rrhh`, pestaña Monitor, en 7 temas (sin tema + azul/cian/manila/noche/esmeralda/terracota) x claro/oscuro x 45 pasos de Tab. (1) Orden de tabulacion: sigue el orden visual izquierda-a-derecha en las dos tablas -- Archivo: Cambiar estado -> Ver archivo (si hay) -> Ver -> Editar -> Compartir -> Eliminar; RRHH: Ver expediente -> Editar -> Imprimir -> Eliminar -- confirmado fila por fila, sin saltos ni huecos. (2) Ningun boton de accion es inalcanzable: todos son `<button>`/`<a>` normales sin `tabindex=-1`, sin `display:none` condicionado a `:hover`, y `renderMonitorTable()` (admin/monitor.js) no oculta ninguno por CSS -- verificado leyendo el JS completo y confirmado en vivo (ninguna parada de Tab se salto una fila). (3) Contraste del outline de foco en esos botones: primer intento con Playwright dio ~460 lecturas por debajo de 3:1 (2.98 constante), pero investigado a fondo resulto ser un artefacto del propio script de medicion, no un bug de la app -- mi funcion de contraste se detenia en la primera capa de fondo no-transparente sin componer alfa, y `.ds-monitor-row` (renglon alterno de la tabla) usa `rgba(0,0,0,0.05)` (paginas.css), que mi script trataba como negro solido en vez de componerlo contra el blanco real de detras. Corregido el script para componer las capas semi-transparentes de verdad (mismo principio que pide CLAUDE.md: "el contraste se calcula contra el fondo real") y repetido el barrido completo: cero botones de accion de las tablas de Archivo/RRHH por debajo de 3:1 en ningun tema x modo -- entre 5,26:1 y 7,04:1 segun fila alterna/tema. Unico hallazgo bajo 3:1 en el barrido, fuera del alcance de esta ronda (no es tabla ni boton de fila): `#ia-burbuja` ("Abrir asistente", widget flotante de IA) a 1,22:1 en algunos temas -- no investigado ni arreglado aqui, queda anotado para otra ronda que cubra el widget de IA en modo claro por tema. Cierre honesto: el eje de teclado/foco en las tablas de monitor (lo pendiente explicito de la ronda 25) queda agotado -- orden correcto, todo alcanzable, contraste real por encima del minimo en las dos tablas. `python -m pytest app/tests -q` no se corrio por no haber cambios de codigo de la aplicacion. | (sin commit, sin cambios de codigo) |
| VI-teclado-outline-temas-ronda25 | agente-ronda25 | 2026-09-08 | terminado: siguiendo lo pendiente de la ronda24 (outline/anillo de foco en los 11 temas + oscuro), se midio con Playwright el contraste REAL (formula WCAG, no solo "existe") del `outline-color` de `:focus-visible` contra el fondo bajo cada elemento, tabulando 14 elementos en `/admin/archivo` con sesion admin.global (localStorage `archive_session`, forma real que exige `checkPersistedSession` en app.js -- `session`/`token` inventados de rondas previas no aplican aqui, sin eso siempre redirige a `/login`) x 6 temas representativos x claro/oscuro. Dos hallazgos reales, mismo patron los dos: el anillo global (`*:focus-visible{outline:2px solid var(--color-link)!important}`, componentes-admin.css L122) es fijo azul oscuro y dos componentes pintan su PROPIO fondo tambien oscuro/saturado, dejando el aro casi invisible pese a que "existe": (1) `.ds-skip-link:focus` (paginas.css) --el enlace "Saltar navegacion", primer Tab de casi cualquier pagina-- ya tenia un blanco de proposito (`outline:3px solid #fff`, con `outline-offset:-6px` para no cortarse por el `overflow` del contenedor, comentario propio citando SD-093) pero SIN `!important`, asi que el generico (que si lo lleva) lo pisaba en todo tema y modo: contraste medido 1,22:1 (WCAG exige 3:1 para foco, 1.4.11), reproducible en los 6 temas probados por igual porque ninguno toca ese fondo. (2) `.ds-admin-tabs .nav-link.active` (pestana activa del panel admin) pinta su fondo con el acento de cada tema (`--color-primary` sin tema, o el color propio de cada uno de los once vía reglas ya existentes) y el mismo anillo azul quedaba encima: 1,19:1 a 2,11:1 en modo claro segun el tema (peor en cian, mejor en noche), sin tema tambien afectado. Arreglado con el mismo principio en los dos: anadir `!important` a la declaracion ya blanca de `.ds-skip-link:focus` (paginas.css, gana por especificidad+!important sobre el generico) y una regla nueva `.ds-admin-tabs .nav-link.active:focus-visible{outline-color:#fff!important}` (componentes-admin.css) -- blanco verificado a mano contra los once fondos de tema documentados en personalizacion.css, el peor caso es noche con 3,34:1 (justo sobre el minimo), el mejor carbon ~9,7:1. Reverificado con el mismo arnes tras el fix: ambos elementos pasan a 8,62:1 (skip-link, constante por tema/modo porque su fondo no cambia) y 3,34:1-12,50:1 (pestana activa, segun tema) en los 6 temas x claro/oscuro probados. De paso, con el mismo barrido se vieron `.ds-toast-close` (2,01:1 en oscuro, fondo gris de Bootstrap `.close`) y el boton de cerrar aviso de error (1,81:1 en oscuro) tambien por debajo de 3:1 -- NO arreglados: son elementos transitorios (toast se autodescarta, ambos sin regla propia en `app/static/styles/`, heredan un fondo de Bootstrap no tokenizado) que ameritan su propio carril, no una tercera correccion de alcance distinto sobre la misma auditoria. El eje de outline/foco de la ronda 24 NO esta agotado: sigue dando hallazgos reales -- el barrido barrio solo 14 paradas de Tab en una pagina; angulo (2) del encargo (tablas de datos/monitor) queda sin probar para otra ronda. `python -m pytest app/tests -q`: 938 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed | 017aec9 |
| VI-ia-burbuja-contraste-ronda27 | agente-ronda27 | 2026-09-08 | terminado: confirmado el hallazgo de la ronda26 con la formula WCAG sobre las reglas CSS calculadas (sin depender de montar la burbuja en el DOM, ya que el arnes no tiene OPENROUTER_API_KEY) -- no era el fondo/icono de `#ia-burbuja` (background:var(--ds-accent)+color:var(--text-inverse) ya pasa AA en los ocho valores de --ds-accent salvo accent-amber en ~3,64:1, no es el 1,22:1 reportado), sino el anillo de foco: mismo patron exacto que RONDA25 (.ds-skip-link/.ds-admin-tabs) -- el generico `*:focus-visible{outline:2px solid var(--color-link)!important}` (componentes-admin.css L122) pinta un azul fijo (#0056b3 en claro) encima del propio fondo de la burbuja (--ds-accent, #2b4e72 sin acento ni dark-mode, ninguno redefine --ds-accent), dos azules oscuros casi identicos: 1,226:1 calculado a mano, coincide con el 1,22:1 medido. Sin `#ia-burbuja:focus-visible` propio no habia nada que le ganara al generico. Arreglado en `ai-widget.css` con `#ia-burbuja:focus-visible{outline-color:var(--ds-accent-on)!important}`, reutilizando el token ya definido para este mismo fondo (SD-048, paneles-admin.css) en vez de inventar un color nuevo. Verificado a mano contra los ocho valores de --ds-accent (:root + los siete data-accent): peor caso accent-amber 3,64:1, resto 6,9:1-12,9:1, todos sobre el 3:1 de WCAG 1.4.11 -- no hizo falta tocar styles/*.css, el patron quedo resuelto solo en ai-widget.css. `python -m pytest app/tests -q`: 938 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed. `python -m pytest app/tests/test_contraste.py app/tests/test_selectores_tema.py -q`: 15 passed | ea238f0 |
| VI-truncamiento-deliberado-ronda28 | agente-ronda28 | 2026-09-08 | terminado sin hallazgos reales, sin cambios de codigo: angulo senalado por la ronda23 (truncamiento deliberado con ellipsis, no el bug de desborde ya cerrado). Grep de `text-overflow: ellipsis`/`_truncBadge`/`title=` sobre `app/static/js/*` y `app/static/styles/*.css` dio 8 sitios reales con contenido: badges de soporte/tesauro (archive.js `_truncBadge`), badges de tipos RRHH (hr.js `_truncBadgeRrhh`), titulo/empleado/cargo-departamento en la tabla de monitor (monitor.js, 3 columnas), titulo en versiones (admin/index.js, 2 sitios) y titulo en vencimientos de retencion (admin_system.html). Verificado en vivo con Playwright contra el arnes (fakedb.py + admin.global) a 390px y 1440px, con datos realistas de fakedb (titulos de 200+ caracteres, badges largos): (a) el `title` SIEMPRE lleva el texto completo sin truncar en los 8 sitios -- confirmado leyendo `el.getAttribute('title')` contra `el.textContent` en cada nodo, sin ningun caso de title ya recortado; (b) donde el truncamiento corta a mitad de palabra (ej. `_truncBadge` con max=28: "El presente documento recog…"), siempre termina en el caracter `…` explicito -- no hay ningun caso de corte que aterrice justo en el limite de una palabra completa dejando ambiguedad sin marcador visual, el `…` senala la continuacion en todos los casos observados; (c) las columnas de tabla con `max-width` fijo (monitor.js: 180-200px, admin_system.html: 180px) usan el MISMO valor en px a 390 y 1440 -- no hay variante mas agresiva para movil, pero a 390px 199px sigue siendo ~50% del viewport, no se veyo un caso donde quede inservible (ej. un solo caracter visible). Unica redundancia encontrada, no bug: en los badges de archive.js/hr.js el JS ya trunca a 28 caracteres con su propio `…` y el `<span>` ADEMAS lleva `overflow:hidden;text-overflow:ellipsis` en un contenedor mas angosto (38-58px) que a veces recorta aun mas el string ya truncado por JS -- visualmente sigue terminando en `…` (CSS reemplaza la cola, no la duplica), asi que no produce doble ellipsis ni texto confuso, solo trabajo redundante. Coincide con el cierre de la ronda23 ("truncamiento intencional, no desborde") pero esta vez verificado en vivo con `title`/`textContent` reales en vez de solo lectura de codigo. Sin brecha de cobertura nueva que justifique tests: la logica pura de `_truncBadge`/`_truncBadgeRrhh` es el mismo hueco de infraestructura (cero tests JS fuera de scanner-app/) que R15 ya documento y descarto por tamano de ronda. `python -m pytest app/tests -q` no se corrio por no haber cambios de codigo. | (sin commit, sin cambios de codigo) |
| IN-integridad-referencial-usuarios-ronda29 | agente-ronda29 | 2026-09-08 | terminado: angulo nuevo (integridad referencial y datos huerfanos) tras leer schema.sql contra el codigo de borrado/purga de admin/catalog.py, trash.py y admin/users.py. Tres de los cuatro puntos del encargo ya estaban bien resueltos (verificado, sin cambios): tipo_documento (catalog.py:delete_category comprueba uso, ofrece fusionar/desactivar/borrar), descriptores_libres (archivo_descriptores/rrhh_descriptores tienen ON DELETE CASCADE en schema.sql, y merge_descriptor limpia explicito), purge_employee (trash.py, comentario propio OA-007/OR-012, ya limpia historial_cargos+documento_versiones+datos_rrhh en una transaccion y se niega a purgar si hay documentos vivos), y audit_log.usuario es VARCHAR (string), no FK, asi que sigue legible tras borrar/purgar un usuario_sistema -- por diseno, no bug. Hallazgo real en el cuarto punto: admin/users.py:delete_user no comprobaba uso antes del DELETE pese a que datos_archivo.creado_por y datos_rrhh.creado_por tienen FK ON DELETE RESTRICT (schema.sql) -- la FK SI bloqueaba el borrado (sin huerfanos), pero database.py convierte cualquier excepcion SQL no prevista en un 500 generico "Error en base de datos" (IN-036) en vez de un error accionable, exactamente el mismo problema que ya se habia resuelto para tipo_documento en catalog.py:delete_category (OA-123/OR-163) pero nunca se replico aqui. Arreglado con el mismo patron: contar datos_archivo+datos_rrhh por creado_por antes del DELETE y responder 409 con el conteo en vez de dejar que la FK reviente con un 500. Test nuevo (test_no_se_puede_borrar_usuario_con_documentos_creados) y el test existente de borrado exitoso actualizado (el mock de db_query paso de un valor unico a side_effect por el conteo nuevo). `python -m pytest app/tests -q`: 939 passed (938 antes, +1 test nuevo) | 2020156 |
| LI-limites-numericos-fechas-ronda31 | agente-ronda31 | 2026-09-08 | terminado: angulo nuevo (validacion de limites numericos y de fecha). Tres puntos revisados. (1) Fechas extremas: `_validate_date` en `app/models.py` solo comprobaba el formato YYYY-MM-DD por regex -- un anio 1800 o 9999 era sintacticamente valido y pasaba sin cuestionarse en `fecha`, `fecha_vencimiento` (DocumentSubmitRequest/DocumentUpdateRequest) y `fecha_jubilacion`/`fecha_pension`/`fecha_nacimiento` (EmpleadoUpdateRequest), confirmado con Pydantic directo antes del fix (`DocumentSubmitRequest(fecha='1800-01-01')` y `'9999-12-31'` ambos aceptados). Arreglado con una cota de cordura de anio (1900-2200) en el propio helper compartido, deliberadamente amplia para no romper fechas futuras legitimas (vencimiento de retencion, jubilacion programada) -- no atada al anio de fundacion real de la facultad. (2) Numeros negativos/cero en paginacion: revisado `paginate()` en `app/utils.py`, usado por archive.py/hr.py/backup.py/trash.py/admin/docs.py -- ya hace `page=max(1,page)` y `per_page=max(1,min(per_page,max_per_page))` con `offset` topado en `max_offset=10000`, asi que `page=-1` o `per_page=-1` quedan clamped a 1 antes de llegar a SQL, no hay LIMIT -1 posible. `admin/catalog.py:get_audit_log` clampa manualmente igual. Sin hallazgo. (3) Fechas invertidas mas alla de nacimiento<ingreso<jubilacion: ya cerrado en ronda 3 (`fecha_nacimiento_coherente`); RRHH tambien valida `fecha_inicio > date.today()` en `hr_alerts.py:214`. Sin brecha nueva en ese punto. Test nuevo en `test_models.py` (`TestFechaValidator`: 6 casos, anio 1800/9999/0100 rechazados en fecha/fecha_vencimiento/fecha_jubilacion, mas 1950 aceptado como control). `python -m pytest app/tests -q`: 944 passed (938 antes, +6) | c9bb3e5 |
| IN-integridad-referencial-restrict-ronda30 | agente-ronda30 | 2026-09-08 | terminado sin hallazgos reales, sin cambios de codigo: continuacion del angulo de la ronda29 (FK ON DELETE RESTRICT sin comprobacion previa -> 500 generico), buscando el mismo patron en las FK RESTRICT que la ronda29 no reviso. Grep de `ON DELETE RESTRICT` en schema.sql dio 8 constraints: fk_tipo_documento_categoria, fk_datos_archivo_usuario, fk_datos_rrhh_tipo, fk_datos_rrhh_usuario, fk_empleado_cargo, fk_empleado_departamento, fk_empleado_estado, fk_historial_cargo. Las dos de `creado_por` (fk_datos_archivo_usuario/fk_datos_rrhh_usuario) ya las cerro la ronda29 en users.py:delete_user (una sola comprobacion cubre ambas). fk_datos_rrhh_tipo ya estaba protegida junto con fk_tipo_documento_categoria del lado tipo_documento en catalog.py:delete_category (verificado leyendo la funcion completa: cuenta uso en datos_archivo+datos_rrhh antes de tocar la fila, ofrece fusionar/desactivar/borrar). El lado padre de fk_tipo_documento_categoria (borrar la fila de `categoria` en si) no tiene ningun endpoint expuesto -- grep de `DELETE FROM public.categoria` sin resultados, las 4 categorias ("partes") son seed fijo referenciado por slug en main.py/helpers.py, no borrable via API. Las tres de empleados (fk_empleado_cargo/departamento/estado) tampoco aplican: admin/docs.py:delete_empleado es soft-delete puro (UPDATE deleted_at, nunca DELETE fisico sobre `empleados`), asi que esas FK RESTRICT nunca se ejercitan desde el codigo. fk_historial_cargo protege a `cargos` (impide borrar un cargo referenciado desde historial_cargos), pero no hay ningun endpoint DELETE para la tabla `cargos` en absoluto (grep de rutas admin sin resultados) -- `historial_cargos` es tabla hija aqui, su propio DELETE (hr_alerts.py:delete_position_history) no toca esa FK. Revisado tambien admin/catalog.py completo (keywords/descriptores con CASCADE, ya cerrado ronda29) y confirmado que no hay mas puntos de `admin/catalog.py` sin cubrir. Angulo de integridad referencial declarado agotado: las 8 FK RESTRICT del esquema estan todas protegidas o simplemente no tienen endpoint de borrado fisico que las ejercite. `python -m pytest app/tests -q` no se corrio por no haber cambios de codigo de la aplicacion. | (sin commit, sin cambios de codigo) |
| SEC-innerhtml-xss-ronda32 | agente-ronda32 | 2026-09-08 | terminado sin hallazgos reales, sin cambios de codigo: angulo nuevo (XSS via innerHTML/insertAdjacentHTML sin escapar), distinto de BUG-onclick-json-sin-escapar (ese fue sobre onclick roto por JSON.stringify sin envolver en atributos, ya cerrado). Grep de `.innerHTML =` / `.insertAdjacentHTML(` en las 18 archivos de `app/static/js/**/*.js`: ~200 ocurrencias. Revisadas a mano todas las que interpolan campos de datos de usuario (titulo, autor, nombre, cedula, notas, motivo, resumen de propuesta IA, resultado de import CSV, etiquetas de vencimiento, palabras clave, historial de conversaciones IA): archive.js (resultados de busqueda, via `hl()`->`highlightTerms()`->`escHtml()`), hr.js (tarjetas de persona y dossier, `escHtml()`/`hl()`), monitor.js (tabla de documentos/expedientes, `escHtml()` en titulo/autor/doc_type, incluido el `JSON.stringify` ya envuelto en `escHtml()` del cierre anterior), edit.js (papelera de documentos y empleados, `escHtml()`), submit.js (ingresos recientes, `escHtml()`), categories.js (`_renderKeywordItem` con `escHtml(kw.nombre)`, consumido igual por `loadKeywordsSection()` e `insertAdjacentHTML` de `handleAddKeyword`), index.js (vencimientos de retencion, `escHtml()`), charts.js (resultado de import CSV, errores con `escHtml()`), app-theme.js (panel de notificaciones, `escHtml()`), ai-widget.js (chat: `formatear()` llama `esc()` ANTES de aplicar markdown ligero y de convertir URLs en enlaces -- comentario propio SI-017/SI-120 documenta la razon; historial de conversaciones con `esc(c.titulo)`/`esc(c.ultima)`). Un solo caso sin escapar de verdad: `hr.js` linea ~217, avatar con iniciales (`${initials}` crudo, de `getPersonInitials()`) -- descartado como no explotable: la funcion siempre devuelve maximo 2 caracteres (recorta con `substring(0,2)` o toma un solo char por palabra), insuficiente para cerrar una etiqueta HTML o inyectar un manejador de evento. El resto usa `escHtml()`/`esc()`/`highlightTerms()` (que ya escapa internamente) de forma consistente. Backend: revisados `app/models.py` (sin sanitizacion de HTML en `titulo`/`autor`/`abstract`/`notas`, guardan texto crudo tal cual lo escribe el admin -- consistente con el comentario de ai-widget.js de que el saneamiento es responsabilidad del frontend en cada punto de salida, no de reescribir el dato en el borde) y confirmado que no hay endpoint que renderice esos campos como HTML del lado servidor (backup export es JSON, PDF de hr.py usa reportlab con texto, no HTML). No se abrio el arnes de Playwright por no haber hallazgo que confirmar. Sin cambios de codigo, sin tests que correr. | (sin commit, sin cambios de codigo) |
| COH-superposicion-arreglos-dia-ronda36 | agente-ronda36 | 2026-09-08 | terminado sin regresion real, sin cambios de codigo: angulo meta tras 35 rondas y 74 despliegues en el dia -- ¿algun arreglo de hoy piso a otro del mismo dia? `git log --oneline --since="2026-09-07 00:00" -- app/` da 35 commits de codigo. Revisados linea por linea los 4 archivos CSS tocados en mas de una ronda hoy: `paginas.css` (4 rondas: fcb5b5d badge-success/info, a072bf9 navbar zoom, 469ea54 .ds-compartido-sello zoom, 017aec9 skip-link focus) -- cada diff toca bloques de lineas distintos, ninguno pisa al anterior; `componentes-admin.css` y `ai-widget.css` (1 toque cada uno hoy, sin superposicion posible); `dark-mode.css` (1 toque, e0fc210, tres selectores nuevos sin colision con reglas previas). `app/models.py` (2 rondas: 3847bdb anadio `_validate_file_url`/limite de password, c9bb3e5 anadio cota de anio 1900-2200 dentro del mismo `_validate_date` que ya existia) -- aditivo, mismo patron, sin pisar nada. Baseline de las tres suites, todas en verde: `python -m pytest app/tests -q`: 944 passed. `python -m pytest app/tests/test_visual.py -q`: 54 passed. `python -m pytest app/tests/test_contraste.py app/tests/test_selectores_tema.py -q`: 15 passed. Verificacion visual manual con Playwright (script ad-hoc, arrancando `main.app` igual que `conftest.py` con `db_query` mockeado): dark-mode + `theme-noche` + zoom de texto 150% simultaneos en `/rrhh` (paginas admin redirigen a `/login` sin BD real disponible, limitacion del harness, no bug) -- sin desborde horizontal, sin errores de consola reales, `.ds-sidebar-brand` con color blanco legible (fix de e0fc210 vivo), `.ds-nav-user-badge` con fondo oscuro propio y texto claro legible (fix de e0fc210 vivo), `.ds-skip-link:focus` con outline blanco 3px `!important` (fix de 017aec9 vivo) -- los tres arreglos de contraste/foco del dia conviven sin pisarse bajo la combinacion mas cargada posible. Conclusion: no hay regresion detectable ni por tests ni por inspeccion visual dirigida; los 35 commits del dia son consistentes entre si. `python -m pytest app/tests -q` no volvio a correr al cierre por no haber cambios de codigo. | (sin commit, sin cambios de codigo) |
| TZ-datetime-utcnow-vs-aware-ronda35 | agente-ronda35 | 2026-09-08 | terminado sin bug real, sin cambios de codigo: angulo del warning `DeprecationWarning: datetime.utcnow()` visto en cada corrida de pytest, investigado como posible mezcla naive/aware. Inventario completo de `app/` (14 usos, 6 archivos): `security.py` no usa `datetime` en absoluto para el TTL de sesion/enlaces -- usa `time.time()` (epoch, sin nocion de huso horario), asi que ese punto explicito del encargo no aplica. `hr_alerts.py` (alertas de jubilacion) tampoco usa `datetime.now()/utcnow()` en Python: TODO el calculo (horizonte, "vencido", dias restantes) se hace en SQL puro con `CURRENT_DATE`/`INTERVAL`, sin ningun valor Python de por medio que pueda mezclarse. Los 14 usos reales estan en `admin/docs.py` (5: `datetime.now()` para defaults de fecha en texto y `datetime.utcnow().isoformat()` para `deleted_at`), `backup.py` (3, timestamps de nombre de archivo y `created_at` de `backup_history`), `admin/imports.py`, `storage.py` y `lookups.py` (1 cada uno, defaults de rango de fecha/carpeta) y `admin/stats.py` (1, umbral de anio). Ninguno se compara en Python contra un valor tz-aware: los que van a columnas `TIMESTAMPTZ` (`deleted_at` en docs.py, `created_at` de `backup_history`) se escriben con `datetime.utcnow().isoformat()` (naive, ya en UTC) y solo se leen despues via `TO_CHAR`/`ORDER BY` en SQL, nunca reconstruidos como `datetime` Python para comparar con algo aware -- funciona porque la sesion de Postgres en Neon esta en UTC por defecto y no hay ningun `SET TIMEZONE` en `database.py` que lo cambie. Confirmado el patron de mezcla NO existe: no hay ningun sitio donde un `datetime.now()` naive se compare directamente contra un `datetime.utcnow()` u otro valor aware dentro de Python. Hallazgo secundario, fuera de alcance de este angulo (no es mezcla naive/aware en Python, es diferencia de huso horario servidor-vs-Caracas): `hr_alerts.py` usa `CURRENT_DATE` de Postgres, que corre en UTC, no en VET (UTC-4) -- entre las 20:00 y medianoche hora Caracas (00:00-04:00 UTC del dia siguiente), el "hoy" que usa el calculo de jubilacion ya rodo al dia siguiente en UTC mientras en Caracas sigue siendo ayer, lo que podria adelantar hasta 4 horas una alerta que cruza el limite del horizonte justo esa noche. No se toco: cambiar `CURRENT_DATE` a `(CURRENT_TIMESTAMP AT TIME ZONE 'America/Caracas')::DATE` en las 8 clausulas de `hr_alerts.py` es una decision de producto (afecta cuando se dispara una alerta de jubilacion en produccion) y el efecto practico es una ventana de 4h una vez al dia sobre un campo de granularidad de dias, no un bug de comportamiento demostrado hoy -- lo dejo documentado para que otro carril lo evalue si se decide que importa. Conclusion: el warning es cosmetico (fecha-a-fecha en UTC-a-UTC, codigo correcto bajo las invariantes actuales), no se "arreglo" nada por evitar sobre-ingenieria sin necesidad real. `python -m pytest app/tests -q` no se corrio por no haber cambios de codigo. | (sin commit, sin cambios de codigo) |
| R37-scanner-app-verificacion | agente-ronda37 | 2026-09-08 | terminado: primera revision directa de `scanner-app/` en si (angulo pedido: rondas previas -E1-escaner-puente, SI-scanner-client-tercer-pase- ya cubrieron 24/107 fichas de `server.js` pero nunca corrieron su suite propia esta sesion ni revisaron `lib/*.js` con ojo nuevo). Confirmado que es el puente local de codigo de barras/QR (no adquiere imagenes), servidor Node standalone fuera del deploy FastAPI, consumido por `app/static/js/scanner/scanner-client.js` via WebSocket+REST con token compartido. Tiene suite JS propia real (`node --test`, unica del repo fuera de Playwright): `test/args.test.js`, `test/hid-decoder.test.js`, `test/redact.test.js`, 18 casos -- corridos antes de tocar nada: 18/18 en verde. Revision completa de `server.js` (485 lineas) + `lib/args.js`/`lib/hid-decoder.js`/`lib/redact.js`: validacion de --port/--mode/--hid-vid/--hid-pid en el borde, --host remoto exige --origin o --dev, limite de payload WS (4096) y de clientes (4), heartbeat con purga, shutdown ordenado en SIGINT/SIGTERM/SIGHUP, body de /test-scan topado en 4096 bytes -- todo ya cerrado por rondas previas, sin brecha nueva ahi. Bug real encontrado en `_tokenValido()`: comparaba el token con `===` sobre strings, que en V8 sale en cuanto encuentra el primer caracter distinto -- en una red local (el escenario de uso real del puente) esa diferencia de tiempo es medible y permite reconstruir el token caracter a caracter con reintentos, exactamente el mismo patron de timing que C9-auth-py-tercer-pase ya documento para el login pero nunca se aplico aqui. Arreglado con `crypto.timingSafeEqual` (comparando longitud primero, sin fugarla como secreto -- solo la longitud del token, no su contenido). Verificado en vivo (`SCANNER_TOKEN=abc123 node server.js --port 3999`): sin token -> 401, token malo -> 401, token bueno -> 200 con /status correcto. `npm test` (scanner-app) tras el fix: 18/18 passed. `python -m pytest app/tests -q`: 944 passed (sin cambios Python, corrido por si acaso). | 3fc637e (repo scanner-app, rama main) |
